# 改动记录与待补

01～06 六份规格文档的**改动记录**与**待补**都收在这里 —— 正文只管「现在该怎么写」；
什么时候改过、还欠着什么，翻这一份。

**判例**（同一份帖文判两遍结果不同的那些帖，逐条等人给裁定）另立一份：[判例.md](判例.md) ——
它不是规格，是规则的立法理由与实例。**判例经验**（这些裁定抽出来的规则、以及要落进 02 的哪一节）
另立一份：[判例经验.md](判例经验.md)。

- **改动记录**：日期 ＋ 改了什么。**新的在上面**
- **待补**：还没定的事。定了的当场划掉（`~~…~~`）并写明结论，行留着不删 ——
  看得到这条路是怎么走过来的

两份都是**流水**，不是规格：写正文时不必读它们，改正文时记得回来加一行。

---

## 01 · 抓取

### 改动记录

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-14 | **重试口径统一**（用户裁定）。三处键与代码一直对不上：代码一律是「连同首次一共 N 次尝试」（`range(1, N+1)`／`range(3)`），文档写「重试 N 次」，读起来像 N+1 次。**改文档不改代码** —— §8 那行「重试 `detail_retry` 次」改成「连同首次一共试 `detail_retry` 次」，§7 停止原因表里「接口重试三次仍失败」同改，§12 的「列表接口重试 3 次」改成「连同首次一共试 3 次」，§8／§12 表里 `scrape.detail_retry`／`market.retries` 的「重试几次」改成「连同首次一共试几次」，`market.retry_delay` 的「重试间隔」改成「两次之间的间隔」 |
| 2026-09-14 | **通读校对第二轮**（六份一起做，只为消矛盾与含糊，规则一条不增不减）。本份十二处：① 一图流⑤ 改「**正常收工才把这次抓取的时刻写进主文件**（§4）」—— 原句读起来像每轮都推进；② §3.1 补出博主本人是谁（**整轮 feed 里出现最多的那个用户**），并补「整轮一条都带不出身份 → 这次抓取作废、不写文件」；③ §4 补出推进抓取截止的条件（**只有正常收工（`since`／`no_more`）才推进**），翻页那条停法改成「主要看……那一日」；④ §5 起点那条补「以及从没抓全过的那种」；⑤ §6 补「总览里『起始』是文件里实际最早的那条帖」，并把三种取不到的情形（库里没有／没有 `source_url`／抓取没成）收成一条；⑥ §8 的 `content` 与风控页两条改写；⑦ 取数节奏表里「info 接口连续空响应」改成「**连续没取到的帖**」（风控页、空手都算）；⑧ §9 表删「帖子数变少」那行、合并两个空字段行，`pubs[0] <= begin_date[:10]` 改成「不晚于起始时间」；⑨ §9 补「**硬失败就到此为止** —— 补充链、报告链、推送链各自抓完就地校验一次」与「**『提示』不等于失败**」；⑩ §10.4 补「（超过 10 个只报个数与首尾）」；⑪ §11 补 `python -m blogger.market --until 2026-09-11`；⑫ §12 那句「本文每一条判据都不写死」改成「下表这些阈值……」，并补出**另有七处写在代码里、不成键**：列表接口重试 3 次、其间隔 3 秒、翻页间隔 1 秒、`has_more` 为假时再连空 3 页、风控重试间隔 5×第几次 秒、视频帖「视频加载中」的字数线 40 字、收盘线 15:00。代码侧：`scrape/verify.py` 的 `verify()` 加 `log` 形参（补齐链用 `print`，报告链与推送链接进自己的日志），`report/flow.py` 与 `push/flow.py` 抓完各校验一次（03§6、06§5.4） |
| 2026-09-14 | **砍掉日线**（与 03 同一轮，缘起见 03 同日那条）。交易日历早已归官方日历、参考价与终点价都走 30 分钟线，日线只剩两件：**行情水位**与**30 分钟线抓漏时的兜底终点价** —— 前者 30 分钟线也能给，后者改成「报错」（03§3.6 ⑤）不再兜底，于是整个撤掉。§10.1 由「两组数据各干什么用」改成一行（30 分钟线落在哪、干什么用，并给出行情水位 = **上证指数 30 分钟线的末根**）；§10.2 删日线那一行（东财 → 新浪 的两级降级随之作废）；§10.4 那张两行的对照表撤掉，改成一句「**每个指数的 30 分钟线末根都要对到「该到哪天」**，逐项对上才算齐」；§11 行情命令的注释由「补到今天并核对」改「补到**该到哪天**并核对」。`data/market/market_data.json` 从库里删掉。代码侧：`paths.MARKET_DAILY`、`market._load_daily()`／`DAILY`、`fetch._fetch_daily()`／`_daily_rows()` 一并删除。代价记入本份待补 #6 |
| 2026-09-14 | **通读校对**（六份一起做，只为消矛盾与含糊，规则一条不增不减）。本份八处：① §3.4 删掉置顶帖那三条解释 bullet，只留「与视频帖同样处置」；② §8 的 `title` bullet 改成「**标题文字不在正文里** —— `title` 丢了就是永久丢了，正文里补不回来」，并新增「取数节奏」表（`detail_retry`／`detail_interval`／`empty_run_pause`／`empty_run_sleep`）—— 这四个键原本只在 §12 表里有名字，正文没有归属；③ §9 硬失败例子「一条帖及其发帖时间都没有」改成「正文与 `pub` 都为空」（前者读起来像两件事各缺）；④ §9「看停止原因本身（§4）」指错了节，改「（§7）」；⑤ §9 `no_more` 行补出它的两种来源（没有下一页／连续 `empty_page_limit` 页没新帖），并删掉与上一行重复的「『帖子数变少』永远是仅提示」；⑥ §10.2 删掉那张表的「为什么」列与「源是外部事实，会变」整句 —— 表里只该有「哪一组｜降级顺序」；⑦ §10.3／§10.4 的「含未来一年」改成「含未来 —— 覆盖到哪天以文件为准，现为 `2026-12-31`」（「一年」是随时间失真的说法），「已过 15:00」补「（15:00:00 算已过）」，§10.4 里重复的那条「日历取不到 → 硬失败」删掉（§10.3 已写）；⑧ §12 修正三处指错节：`scrape.max_pages`／`target_posts`／`empty_page_limit` 由 §3 改 §7，`market.sina_datalen` 由 §10.1 改 §10.2 |
| 2026-09-14 | §10.4 删掉**推送链那一行**与「推送链只比 30 分钟线这一样」句 —— 推送的注记取现价、与 30 分钟线无关，也不走这一套备料。核对只剩报告链一条口径（交易日且已过 15:00 → 运行当日），「比两样」也归报告链。原写法（推送按本档时刻该覆盖到哪根 30 分钟线核）是 09-13 照搬报告链口径定的，把那句话与本文的「比两样」「推送链只比这一样」凑在一起本就自相矛盾 |
| 2026-09-13 | 定案：**§10.4 推送链要的那根 30 分钟线改用「结束时刻不晚于本档时刻的最后一根」**（原写「本档时刻所在那根」）—— 后者在 `09:30` 与 `13:00` 两档上当天根本还不存在（第一根要到 `10:00`、下午要到 `13:30` 才收出来），那两档每天都会白白硬失败。同时写明**推送链只比 30 分钟线这一样**：卡上不显示行情，日线对推送没有用处（06§5.3） |
| 2026-09-13 | 定案：**§10.3 交易日历改用官方日历**（含未来一年），不再拿「有上证行情的那天」倒推 —— **日历与行情拆成两件事**：日历说哪天开市（数日子认它），行情说数据到哪天（算价认它）。合在一起时，行情缺一天会被日历悄悄吃掉（那天既算不出价、也数不进日子）。副作用两条：① 长假期间（非交易日的工作日）现在也能用上一交易日收盘当现值了；② **「当月最后交易日」不再被截到日历末日** —— 实测一位博主 09-02 发的 `month`，旧日历答 09-11（已收盘→计分），新日历答 09-30（未到→待验证），是**旧的那份算错了**。10.4 的「比三样」相应改成**比两样**（上证已含在每个指数里）；日历取不到由「退回只跳周末」改为**硬失败** |
| 2026-09-13 | 定案：**§6 的两条命令定名** —— `python -m blogger.scrape.align`（对齐总览，只读、不联网、不写盘）与 `python -m blogger.scrape.fill [博主名]`（补齐，不传＝全库）。两条此前在 §11 里一直标着「尚未实现」，暂定名 `align_posts` 作废（待补 #4 结案） |
| 2026-09-13 | 定案：**补齐内部逐个校验** —— 每补完一位，用这位自己算出的起点调一次校验（§9）。起点是补齐算的，只有它知道该传什么；一把跑完直接报出「谁补齐了、谁硬失败」 |
| 2026-09-13 | **新增 §10 备料：行情** —— 行情此前只在 03§2.1 一句「从哪来」，没有一份文档写它**怎么算齐、怎么核对**。今天东财整片不通、日历静默停在上一个交易日而报告看着一切正常，正是缺这一段。四小节：两组数据各干什么用／从哪来（逐源降级）／交易日历怎么定／**抓完必须核对**（另取官方日历当尺子，不达标非零退出）。原 §10／§11 顺移为 §11／§12 |
| 2026-09-13 | **新增 §11 可调参数** —— 本文用到的键（`scrape.*` 9 个、`market.*` 4 个）从仓库根的 `config.json` 读，正文里不再写死 |
| 2026-09-12 | 定案：`stop_reason` **加第三种 `error`** —— 接口重试三次仍失败单独盖章，不再混在 `no_more` 里冒充正常收工。**只有正常收工（`since`／`no_more`）才推进抓取截止**，否则下次从今天续、把没抓到的中段永久跳过 |
| 2026-09-12 | 定案：`stop_reason` **再加第四种 `capped`** —— 翻到页数上限／条数收够，是我们自己截断的，`no_more` 只留「feed 真的翻到底了」。两种都不推进抓取截止。§9 判 `capped` 为**硬失败**（还拿得到更早的帖，只是没拿） |
| 2026-09-12 | §9 覆盖判据改为**只看停止原因本身**：`since` = 覆盖到位、`no_more` = 仅提示、最早一条正好落在起始时间当天 = 覆盖到位。**删掉折天数复核**「一页以上的量」—— 那个量折算成几天没有可执行的算法，遇周末与长假必然假报硬失败 |
| 2026-09-12 | ~~§3.2 `stop_reason` 的 `no_more` 释义改成「没碰到起始时间就停了」~~ —— **同日推翻**：实测它盖着三件事，正确的修法不是改释义而是**拆章**（`capped` / `error`），`no_more` 回归「feed 真的翻到底了」。见本表首两行 |
| 2026-09-12 | 定案：**起始时间统一叫 `begin_date`**，只有这一个参数（删掉试跑用的月份换算法）。抓取与校验**传同一个值**，覆盖检查才判得了 |
| 2026-09-12 | 定案：**起始时间是硬边界** —— 早于它的帖一律不入库。**翻页每页全收、收完统一过滤一遍**（不逐条判边界），因此边界页上那几条早于起始时间的帖会一起丢掉。**过滤只作用于这一轮收下来的帖**，不削历史。§0 补一句：这一层做干净了，下游不必再筛 |
| 2026-09-12 | 定案：**置顶帖不入库**，按列表条目上的置顶标记识别 —— **可能有好几条**，日期任意。理由：它是公告位、插在列表最前面，搅乱倒序翻页，并让 `time_range.earliest` 失真。与视频帖同样处置（判定即丢、只记已完成记录） |
| 2026-09-12 | §1 一图流改为「四道关」；丢掉的东西一律**不留痕**（不入库、不计帖数、不写占位）统一写在 §1 末尾 —— 此前 §1 说视频帖「只写占位标记」，与 §8「不留占位」自相矛盾 |
| 2026-09-12 | §1 一图流把「收完统一过滤」提到「取详情页」**之前** —— §4 原本就写的是「翻页时每页全收，收完再统一过一遍」，一图流的顺序与它不一致。反正要丢的帖，不必为它再取一次全文 |
| 2026-09-12 | §3.4 把置顶标记**指名成 `is_stick`** —— 与 §3.2 指名 `publish_time` / `behot_time` 体例一致。实测：只有置顶的那条带这个键，别的条目压根没有 |
| 2026-09-12 | §8 补：**微头条不走详情页**，列表给的文字就是全文；逐级降级只适用于**文章类帖** |
| 2026-09-12 | ~~§9 硬失败判据改为「`stop_reason` 说是抓到边界了、但最早的一条离起始时间还差一页以上的量」~~ —— **同日推翻**，见本表首行。过滤之后文件里不可能再有越界帖这一点仍成立（旧判据「最早的一条晚于起始时间」确实会误报） |
| 2026-09-12 | **立稿**（新结构下的 01，取代 `legacy/06-抓取.md`）。新增 `§3 三道关`：身份 / 时间诚实 / 列表页噪声 / 文字够长 —— 身份关与时间诚实关旧文档完全没写 |
| 2026-09-12 | 定案：字段名写实名（`scrape_time` / `title` / `content` / `publish_time` / `stop_reason` / `time_range` / `user_info`） |
| 2026-09-12 | 定案：身份关**认不出就拒**，不用历史重叠背书；**删 `total_posts`**（等于 `len(posts)`，实测 2/162 个文件对不上）；~~`stop_reason` 只留 `since` / `no_more` 两种~~ —— **同日改成四种**，见本表首两行 |
| 2026-09-12 | 定案：微头条**没有标题**，列表文字落进 `content`；`content` 必填、`title` 可选（文章类帖才有）—— 旧文档「微头条正文留空」写反了 |
| 2026-09-12 | 定案：**取消「补正文」分步工序**，标题与正文在抓取命令内部一次拿齐；`title` 来自列表、`content` 来自详情页 |
| 2026-09-12 | 定案：视频帖识别**加链接域名 `toutiaoimg` 作第四条判据**（旧文档待定 #41） |
| 2026-09-12 | 输入明确为**博主的任意一条帖子链接**；`data/posts/` 下只放 `<博主名>.json` |
| 2026-09-12 | §6 定案：**补齐是手动动作、不自动触发**，起点就是续抓起点，删掉「多远算远」的判定；§0 补「抓取从不自己发生」 |
| 2026-09-12 | 待补定案：认不出身份的量**暂不改**；风控（限速/重试/间隔）留到写代码时设计，硬要求是不能被封；旧数据**全库重抓**，残留分片随重抓消失 |
| 2026-09-12 | 定案：**视频帖不入库** —— 判定即丢，不写主文件、不计帖数、不留 `[视频帖]` 占位。此前是「照常存进库里、由下游决定跳过」。下游因此不必再判一次 |
| 2026-09-12 | 定案：**删掉「文字够长（不足 5 字丢掉）」这一关** —— 三道关就是三道，与 §1 一图流一致 |
| 2026-09-12 | 定案：**删掉 §1 一图流末步「扫一遍 scrape_time 自动补一段」** —— 与 §6「补齐是手动动作、不自动触发」矛盾 |
| 2026-09-12 | 定案：**抓取是区间式的** —— 抓 `[起始时间, 至今]`，**抓多少就解析多少**。起始时间一律由**调用环节**算好传进来，抓取自己不读 `scrape_time`、不判断该抓多少 |
| 2026-09-12 | 定案：**新增补齐命令**（名字见本表首两行）—— 「该从哪续」的判断从抓取脚本移到补齐环节；抓取只剩一个参数，原来的 `--align` 开关取消 |
| 2026-09-12 | 定案：**发帖时间字段统一成 `pub`** —— 帖子里的 `publish_time`（Unix 秒）与 `publish_date`（字符串）合并为一个 `pub`。`publish_time` / `create_time` / `behot_time` 是**头条接口的字段名**，不是我们存的名字 |

### 待补

| # | 待定的事 |
|:---|:---|
| 1 | **认不出身份的条目有多少**：§3.1 定的是「认不出就拒」。写代码时留意这个口径会拦掉多少条（尤其 feed 本身不带 user 的博主），量出来后再看要不要留后门。**暂不改** |
| 2 | **风控**：§8 的详情页重试，以及整条抓取链的请求节奏 —— **限速、重试上限、间隔**都留到写代码时设计。硬要求是**不能被封** |
| 3 | **旧数据全部重抓**：`data/posts/` 里 9 个 `_bodies_s*.json` 分片是旧「补正文」工序的残留，与 §7「只放 `<博主名>.json`」不符。计划是**测试通过后全库重抓**，残留随重抓一并消失 —— 不单独清理 |
| 4 | ~~补齐命令的名字待确认~~ —— **已结案**：命令是 `python -m blogger.scrape.align`（对齐总览，只读）与 `python -m blogger.scrape.fill [博主名]`（补齐）。补齐**逐个校验**，不靠手动跑 `verify`（§6、§11） |
| 5 | **旧帖里的 `publish_time` / `publish_date` 怎么迁**：§7 已定成 `pub` 一个字段。存量 172 个文件都是旧形态 —— 若走「全库重抓」（待补 #3），随重抓自然消失；否则要单独迁一次 |
| 6 | **30 分钟线只剩新浪一个源**（2026-09-14 砍掉日线后）。原先日线有东财 → 新浪两级，某指数某天抓漏时还能拿日线收盘兜个底；砍掉日线后，30 分钟线自己**没有降级余地** —— 抓漏就是「报错」（03§3.6 ⑤），不兜底。要不要给 30 分钟线补第二个源，留到写实现时看实测的抓漏率再定 |


---

## 02 · llm解析

### 改动记录

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-14 | **重试口径统一**（见 01 同日那条，用户裁定）。§11 表「重试 `parse.max_retries` 次」改成「连同首次一共试 `parse.max_retries` 次」，§12 表 `parse.max_retries` 的「重试几次」改成「连同首次一共试几次」、`parse.retry_delay` 的「重试之间的间隔」改成「两次之间的间隔」 |
| 2026-09-14 | **规则文本按本文整份重新生成 ＋ 五项裁定落地**（用户裁定「现在一起做掉」）。① §3.1 新增两行：「收阳／收阴」**算**（单一收法就是涨跌的同义表达，`1`／`-1`）、「无净方向收法（『收小阳线或十字星』）」**不算**（两种收法摆在一起，等于没选）；② §4.3 补「**日期与周期词同现时跟周期词走** —— 『本周到 8 月 20 日』落 `week`，不落 `d:`」；③ §5.4 与 §10.1 里写死的 `±10%` 改成 `± parse.band`（保留「现值 0.10 —— 现值 3900 时即 3510～4290」作为举例）；④ §10.2 新增取用程序表（多数一致／略有出入／只有一份提到／一份带着别份没有的产出，四种情形各取哪一份），外加「每条都要与 N 份里的某一条逐字一致、不改 `d`、不做乘法、不凭空补」；⑤ 删掉「若博主嘴上写『本周』而语境分明是预判即将到来的那一周 → 落 `nweek`」整条 —— **判读严格守字面**，不再替他重解语境。代码侧：`parse/prompts.py` 按本文**整份重写** —— `BAND`／`QUOTE_LIMIT` 由 `params` 读入并渲染进规则文本（占位 `__BAND__`／`__BAND_RANGE__`／`__QUOTE_LIMIT__`／`__DOCTRINE_NO_DIRECTION__`），新增小节「三元组：三个分量在同一层」，开篇任务陈述改成八个指数，行情注记段补「只给七个主指数，双创不单列」与注记头三种形态；同时删掉本文没有的「拼凑／」「行情回顾／新闻评论」「整理蓄势」，删掉 `cat`／`summary`／方向强度，`long` 行改成本文的封闭枚举、点位那行去掉「＋ 周期」。**规则指纹随之由 `603c3a6437ec77ef` 变为 `9ff2cf78ce60f200`，八份判断缓存全部作废，已跑一次全库重判**（`python -m blogger.compare_all`） |
| 2026-09-14 | **跨帖不去重，定为现状**（补一行进 §7）。缘起：挖归档 SKILL.md 的负例时发现它第 105 行有一条「**同一天发布的针对同一指数同一预测周期的同方向预测只记最新一条**」，**新栈里没有这条规则的任何落点** —— §7 只管「一条帖能出几条」，§10.1 那条去重明写「**同帖内**」。**实测**：全库 1494 条信号里 **152 条**（10.2%）落在「同日同指数同周期同方向」的重复组里（云帆观市 60 组、故乡的云 25、诸葛不亮 22、江河 20、山顶 3、荷叶 1、知行 1、我觉醒了 0）。**裁定：不捡。** 每条帖是一次独立的、带自己参考价的表态 —— 博主盘中看到行情变了再喊一次，参考价不同、算的分也不同，硬合成一条是丢信息。**定案记进 §7 表**（「同一天的另一条帖说了同样的话 → 不去重 —— 本文只判一条帖，两条帖各算各的」），免得下次再被当成漏的。**附：捡回来会动什么**（留档，供日后翻案）—— 计分信号 1494 → 1342；03 的五个数：云帆观市 332/0.43 → 262/0.48、故乡的云 128/0.15 → 112/0.04、山顶 55/0.32 → 51/0.21、诸葛不亮 247/0.01 → 221/−0.05；**04 总榜 7 上 1 尾 → 6 上 2 尾**（退榜的是山顶望星空的诗人，上证均分 0.21 → 0.08，跌破 0.1 资格线）。回测不受影响 —— 05 取「每个博主每个交易日最多一个波段观点」，本来就只留一条。纯文档，`prompts.py` 与指纹不动 |
| 2026-09-14 | **§6 补负例**（源：`archive/20260914-analyze-blogger/SKILL.md` 第 119 行与第 131～137 行那份「不产生信号」清单）。那些条文当年只落进了 `prompts.py`，没回写进本文 —— 结果**规则文本里的例子比本文多**，本文 §6 一直是薄的那一份。三处：① §1.1 加一行「疑问句／引流式标题 ＋ 正文里的结论」（「明天A股会怎么走？」「下周要变天？」）—— 原本只在 §2 的「标题＋正文」行提过一句、没有例子；② §4.1 引子那句「帖里出现「明天」二字，不代表这就是对明天的预测」补括注例子「（「明天开始至本周五」说的是本周，「明天」只标起点）」；③ **§6 表逐格补**：① 缺方向 —— 条件式补「守住3900看反弹」「放量突破才追」「冲高回落转守4000」「接下去每跌50点加0.5成」，模棱两可补「不好说」「看市场情绪」，仓位状态自述补「我拿着不动」，**新增一行「观察句」**（「明天会怎样，先看4000反击」）；② 缺周期 —— 纯复盘补「今天这波下跌说明……」「这次探底回升很标准」，状态／背景句补「目前处于上升趋势」。**一处是反转**：那条「明天会怎样，先看4000反击」本日早些时候刚从 §4.1 删掉（理由「上面那句已经把话说完了」），现在**换了去处**放回 §6 的「观察句」行 —— §6 整节的职责就是装负例，落到这里不是重复。**规则文本这一批不动**（`prompts.py` 第 14／29／45～51 行本就带着其中大部分例子，是本文此前反而少了），指纹仍 `603c3a6437ec77ef`、**判断缓存不触发重判**；两处新增情形进待补 #6 一并收口 |
| 2026-09-14 | **`parse.runs` 补进判断缓存的指纹**（§11 末句、§12 末条）—— 缘起、实测与全部五处改动见 03 节同日那条。改「一条帖跑几遍」此前不作废缓存，会静悄悄拿回旧参数下的结论 |
| 2026-09-14 | **通读校对**（见 01 同日那条的缘起）。本份九处：① §0 表首行补「回测经 04 那一步读（05§8）」—— 否则「谁调」一列看不出回测也走这里，「同上 ——」改成「03／06 ——」；② §1.1 那条 blockquote 收成引子里一句话（表下不挂裸 blockquote）；③ §4.1 两条含糊的「含『本周初』」「含『下周初』」改成实指「**『本周初』→ `monday`**」「**『下周初』→ `nmonday`**」—— 只写「含」等于没说落在哪一档；④ §6 ② 例子里的「整理蓄势」删掉 —— §3.1 已把「蓄势」归 ① 不算方向，例子拿它当方向句会读反；⑤ §9／§10.1 两处写死的「≤ 60 字」「超过 60 字｜截到 60 字内」改成 `quote_limit`（§9）—— 正文里写死现值，配置一改就成假话；⑥ §8 JSON 示例的 `post_id` 换成一个真在库里的号（原来的 `1874751803693056` 在 `data/posts/` 里查不到，示例抄不动）；⑦ **修一处事实错**：§1 示例原写「2026-09-01 **周一**」，09-01 是周二；⑧ §11 引子补「**规则文本就是本文 §1～§10**，落在 `blogger/parse/prompts.py`」—— §0 有一处指向 §11 的引用，原先到 §11 只看到一张参数表；⑨ §12 的「（03§8 也列了它们）」改成「（03§2.2）」（那四个键的归属在 §2.2，03§8 只是转列） |
| 2026-09-14 | §6 表 ② 缺周期 ·「过期」一格的处置由「见 §4.2 的**三条 spec** 拦截」改成「见 §4.2 的**拦截表**」—— §4.2 那张表现在是**四行**，首行「验证终点落在非交易日」管的是「说的那天没开市」，与 `pub` 比收盘时间无关，**不是 spec 拦截**（spec 拦截是后三行），旧措辞既数错了数也说窄了范围。**这是继承来的老毛病**：730bcdb 那版旧 02 里 §4.2 只有一行（拦「任何档位」），「三条 spec 拦截」在那时就已经对不上 |
| 2026-09-14 | **「一条帖跑几遍」由 03 移归本文** —— 跑几遍、以及跑完由模型定夺最终取哪一份，都是「解析一条帖」这个动作的一部分，N 不该由调用方给。§10.2 那句「N 由调用方给（03§2.2）」改成「N 取 §12 的 `parse.runs`（现值 1）」；§12 可调参数表加一行 `parse.runs`（管什么：一条帖跑几遍；本文哪一节：§10.2），排在自己的 `band`／`quote_limit` 之后 —— 此前表里根本没有这个键，而它是解析自己的参数。§12 底下那条归类 bullet 由「`band` 与 `quote_limit` 是本文自己的判据」改成「`band`／`quote_limit`／`runs` 是本文自己用的」。**03 侧同步**：§2.2 表删掉「一条帖跑几遍｜1 遍（见 02§10.2）」那一行（那张表只剩批与缓存的事）；§7 两行示例命令去掉 `[--runs 1]`、底下 `--runs N` 那条注记一并删掉 —— 定案：**值只在配置里，命令行不开这个口子**（与 05§8「要改这些就改配置，不在回测命令上开口子」同一条规矩）。05§8 那句跟着由「一条帖跑几遍 `runs = 1`（02§10.2）」改成「一条帖跑几遍走 `parse.runs`（02§12）」。**代码同批同步**：`config.json` 的 `parse` 一节加 `runs: 1`（排在 `quote_limit` 之后，与 §12 表同一处相邻）；`extract.py` 加模块常量 `RUNS = params.get("parse.runs", 1)`，`extract()` 与 `parse_one()` **去掉 `runs` 形参**（值只有配置一个来源，调用方给不了），函数体里读 `RUNS`，那条 `runs < 1` 守卫改成校验 `RUNS`；`report/flow.py` 与 `compare_all/flow.py` 里两个写死的 `RUNS = 1` 删掉，`report_one()`／`report_all()`／`judge_posts()`／`pool.update()`／`compare_all.run()` **一路上不再传 `runs`**；`backtest/flow.py` 的 `RUNS = 1` 删掉、改调 `cmp_pool.update(BEGIN_DATE, True, log)`；`report/__main__.py`、`parse/__main__.py` 的 `--runs` 解析删掉 —— 四条命令行现在都把 `--runs` 报成「认不得的参数」。**顺带补一道闸**：`parse/__main__.py` 原先没有「未知参数」这一支，删掉 `--runs` 后 `--runs 3` 会把 `3` 当成博主名，故补上（与 `report/__main__.py` 一致）。实测：`compileall` 过；`extract.RUNS = 1`；默认路径（`RUNS=1` 带自查）走「解析一遍 → 定夺」，关掉自查只解析一遍，`RUNS=3` 走「解析三遍 → 定夺」，`RUNS=0` 触发 `parse.runs 至少是 1`；四条命令行 `--runs 3` 全部 rc=2 |
| 2026-09-14 | **删除顺延机制：验证终点就是博主说的那一天**（推翻本日上一条「`today` 只收窄一档」，也推翻 09-13「整周休市时终点顺延」定案）。**为什么顺延不能留**：终点被往后挪，若原本终点在发帖日**之前**、顺延后落到发帖日**之后**，一条本该判「无效-过时」的信号就变成「计分／待验证」—— 凭空多一条错的。（机制上确实存在：2016–2026 全扫 10,611 例；本库语料里 0 条。）**新口径**：终点落在非交易日（周末、节假日）→ 这条表述没有可验证的终点 → **不成立**（02 不产、03 判「报错」）；整周、整月一个交易日都没有的，同理 —— 那一格说的「最后交易日」根本不存在。**文档**：§4.1 `d:` 行终点列「该日期，非交易日顺延」→「该日期」；表后口径段改成「**验证终点就是博主说的那一天，不顺延**…」；§4.2 拦截表首行由 `today` 专用的「pub 在非交易日」**通用化**为「验证终点落在非交易日」（`today` 的终点就是发帖当天，是它的一个实例）。03§3.2 引子、§3.3 表第 3 行（「终点顺延、基准日、跨度全依赖它」→「终点**是不是交易日**、基准日、跨度全依赖它」）、§3.6 ② 行、05§2 表同步。**代码**：`market._settle()` **整个删掉**；新增 `after_calendar(d)` —— 把「晚于日历末日、现在判不了」与「日历里明确是休市」分开（前者仍归「待验证」，后者才是「报错」），`is_trading_day()` 对两者都只说 False，混在一起会把待验证误判成报错；`_last_td_of_week()`／`_last_td_of_month()` 空集分别退到该周周日／该月最后一天（自然日，新增 `_last_day_of_month()`）；`endpoint()` 的 `today` 分支改 `return day`、`d:` 分支改 `return target if _ok_date(target) else None`、两个周几分支去 `_settle`；`verify.py` `_state()` ② 由「`spec=today` 且发帖日不是交易日」改成通用判据「`ep` 没超日历末日 且 不是交易日 → 报错」。**实测影响恰好 3 条**，全部由计分／待验证／无效-过时变**报错**：云帆观市 2026-02-12 21:36 `nfriday`（终点 02-20 春节休市，旧口径顺延后**误算成计分**）／山顶望星空的诗人 2026-08-27 19:45 `d:2026-09-20`（周日）／江河之水终有入海之日 2026-03-01 11:24 `monday`（终点 02-23 春节休市）。三条**都是模型落错档**，新口径把它们翻出来当报错，是改善。新状态分布：计分 1246／不计分 140／报错 3／待验证 21／无效-过时 4。**规则文本同批同步**，三处：① 第 100 行去「非交易日顺延」；② 第 104 行去「（顺延由系统算，你只落档）」；③ 那张「收盘后」拦截表**补上本文 §4.2 的第一行**「验证终点落在非交易日 → 说的那天没开市，不成立，不产」，排在收盘后三行**之前**（与 §4.2 表、03§3.6 ②在③之前的次序一致），小标题跟着由「周期词要看『发帖时那一段走完了没有』」改成「**周期词的两种不产**」、引子改成「两种情形不产：说的那天没开市／发帖时那一段已经走完」—— 原来那张表只装得下第二类。**代价仍是零**（缓存早已作废，见下），只是第 ③ 处**上一次是被专门推迟的**：09-14 上一条记「补『非交易日』那条归待补 #6，免得白作废 4149 条判断」，那个理由随缓存作废而消失，故本次一并补上。指纹 `cea41822ad305e9b` → `eee300513d393f20` → **`7a5ff7f4833806ee`**。**这次同步的代价是零**：`data/state/parse_cache/` 8 份缓存的 `rule` 全是 `df9491691f149a08`，与改前的 `cea41822ad305e9b` **本就对不上**，**4149 条判过的帖早已全部作废**，下次跑报告无论如何都要整批重判。`next_td()` 现在在 `blogger/` 里没有调用者，保留未删 |
| 2026-09-14 | **`today` 的成立条件定死：必须交易日发布，且必须在收盘前发布 —— 缺一不成信号**（推翻 09-13「未采纳远端第 ⑤ 条『非交易日发布不视为信号』」那条 —— 当时驳回，是因为它听着像把**所有**档位一竿子打死，与 03§3.4「发帖日不是交易日 → 基准往前取最近一个交易日」相抵。**收窄到 `today` 一档就不冲突**：周六说的「今天」不是对任何一个交易日的预测，这话本身不成立；周六说的「下周」照常是有效信号，基准照常往前取周五。所以 03§3.4 那条一个字没动）。**文档**：§4.1 `today` 行的括注由「必须交易日的收盘前发布」改成「它只认**交易日收盘前**发布的，非交易日发布或收盘后发布都不成信号，见 §4.2」；§4.2 那张拦截表**补第一行**「pub 在非交易日 且 spec=`today` → 『今天』得先有个今天 —— 这话本身不成立 → 不产」，排在「交易日收盘后」那行之前，与 03§3.6 的判定次序（② 报错 在 ③ 无效-过时 之前）对齐；表后补一句「前两行合起来就是 `today` 的成立条件」。**代码**：`market.endpoint()` 的 `today` 分支**不再顺延**（原 `day if is_trading_day(day) else next_td(day)`，注释写的是「顺延只是防御，别让它崩」）→ 非交易日返回 `None`。顺延不是防御，是把它当成一条**挪了终点的有效信号**：`ep`／`epc`／`span` 都在，`ret`／`score` 还照算 —— 周六说「今天」，`ep` 会落到下周一。改完实测：周六 `10:00` 说「今天」→ `ep`／`epc`／`span`／`ret`／`score` **全 `None`**、`note=报错`；交易日盘前／盘中 → 计分；交易日 `15:30` → 无效-过时（03§3.6 ③ 那道不动，边界照旧取等）；`d:` 的顺延不受影响（落周六仍顺延到周一）。**对现有数据零影响**：全库 1414 条信号里 `today` 落在非交易日的 **0 条**（模型本来就没这么产），所以不触发重判，`reports/` 也无须重刷。规则文本 `prompts.py` **不动**（第 89 行已写「必须交易日的收盘前发布」，第 118–121 行只列了收盘后那三种）—— 补「非交易日」那条归待补 #6，免得白作废 4149 条判断（指纹仍 `cea41822ad305e9b`）。03 侧 §3.6 ② 本就是这条，未动 |
| 2026-09-14 | **02 全篇结构重排**（472 → 432 行）。立下四条体例：① 一节的规则**只以两种形态出现** —— 表里的一行，或引子里的一句话；**表下不再挂裸 bullet 与 blockquote**。② 引子只写「这节在问什么」与「跨全表的通用口径」（交易日、边界取等、顺延）。③ 特例一律落成表里的一行，不单开小节。④ 不写「为什么」。逐条落地：§1 的「为什么要互参」表删掉、压成 §1.2 引子一句；§2.2「为什么不许取不到就跳过注记」整段删；§4.2「模型难免编错，两道都要」删；§2.1 表下两条 bullet 收进表里两行（并把 09-13 那版含糊的「所处那根的开盘价」并成一处口径，以精确的那句为准）；§4.1 表下四条 bullet 归位（两条进引子、X-Y 天进 `tN` 行、顺延进表后一句）；§3.1「其余取值一律丢掉」与 §9 的 blockquote 各收成表里一行；§3.3 两条负例并进引子；§5.1 的八个对象由 blockquote 改正文、删掉与 §5.2 逐格重的「别名归一」；§5.3 两条与 §7 表重复的 bullet 删；§6 末句「机构/券商不是本人表态」并进「转述他人」格。**三处结构动作**：① 一图流从 §1 中段迁到 **§0.1**（六份文档里只有 02 不是「图靠前」的体例）；② §4.2 末尾那串「一条帖里挑哪一句」升成 **§1.1 核心预测句**（它与 §1 那句「先定位核心预测句」本是同一条规则，挂在 §4 下是错父）；③ **§5.5「上证的背景句不成三元组」撤销**，并入 §6 的「① 缺方向」当一行 —— 随后再合一次：该行与「② 缺周期」的「状态描述」本是同一个上位类，**并成一格「状态／背景句」**（「说现在是什么样，或主谓不是在预测上证未来涨跌」），§6 由 9 行回到 8 行。**两处评审建议删而判定保留**：§4 起首的「『档』在本文＝周期档位／推送那个『档』是时刻表上的一个点」——是术语消歧不是「为什么」；§10.2 的「拦什么｜怎么认」表——是 reconcile 那一步自己的动作清单，与 §3.2／§4.2 的判据不同层。**顺手修一处事实错**：§12 正文写「`model` 到 `call_deadline` 六个归 §11」，`config.json` 里是**五个**。**规则一条没减** —— 12 字片段比对旧稿 288 句，29 条无落点逐条裁定（28 条是有意删的「为什么」与重复，2 条是误删的从句，已补回：§5.4「不是模型记忆里的旧点位」、「比大小时取等、边界一律归严的那一侧」）。编号除新增 §0.1／§1.1／§1.2 外未动，**外部引用零改动**。纯文档，`prompts.py` 与指纹都不动（仍 `cea41822ad305e9b`） |
| 2026-09-14 | **§6 末尾两处挪进 §4.2、一处删掉**（连同上一条那条解释，§4.2 底下现在是一串「一条帖里挑哪一句」）：①「**「先抑后扬」在语料里是高频表述**，绝大多数是看多路径（形态半句 + 回踩买入半句）。不许因为它是形态就把这类真看多帖整条漏掉；也不许把「先抑后扬」自行解读成看多」（blockquote 原样带过来）；②整条帖立场含糊但夹带明确预测句 → 捕捉那句（「后市难料，但明天我看涨」→ 捕捉「明天看涨」）。**三处是挪不是抄**，§6 原处已清空 —— §6 末尾现在只剩表格与「机构/券商的看法不是博主本人表态」。另一处「**引文必须连同方向词一起截**，不许只截前半句形态」**同日删掉、没挪**（这条在规则文本里对应第 17 行的「quote **从方向句起逐字截取、必须含方向词**（**不得**只截前半句形态）」与第 18 行尾的「quote 取方向承载句，不得把形态改写成方向」—— 按待补 #6，重生成时一并收口）。与 §4.2 首句「同一条帖既有复盘又有未来预测 → 只捕捉未来预测那部分」合起来读：**一条帖里挑出真正承载预测的那一句**。规则文本这一批不动（见待补 #6） |
| 2026-09-14 | **上一条同批落进规则文本**：`prompts.py` 第 18 行的**放宽句整段删掉**（「该周期若另有下列任一，则以那一句成条——（i）独立的无条件方向句；或（ii）明确的逢跌买入承诺…」），紧跟的那句改成与本文 blockquote 逐字对齐（「「先抑后扬」在语料里是**高频表述**，绝大多数是**看多路径**（形态半句 + 回踩买入半句）」）。定案：**规则文本与本文严格一一对应 —— 本文没有的，规则文本也不写**。两条放宽的实质在规则文本里另有落点，没减东西：第 17 行「同期形态 + 独立方向句 = 产」＝（i），第 42 行「操作动词 = 方向」（加仓/重仓/补仓/逢低买/抄底 → 看多）＝（ii）。**代价照实说**：规则指纹 `303ec21661325fe9` → **`cea41822ad305e9b`**，`data/state/parse_cache/` 8 份缓存、**4149 条判过的帖全部作废**，下一次跑报告要整批重判（全库 1414 条信号会跟着重算） |
| 2026-09-14 | **删掉 §6 的「两条放宽」小标题与那张两行表**（09-13 记的「未采纳远端第 ④ 条」就此反转）。当时不删的理由是「删了『形态句 + 独立方向句』这类真看多帖就不再产」，**这个理由不成立**：那两条本身不是新规则，是既有规则在形态句这一处的展开 —— （i）「另有独立无条件的方向句就以它成条」已写在 §6「周期词不救形态」那一格（「没有一句独立无条件的方向句就不产」），（ii）「逢跌买入承诺」已写在 §3.3「操作动词就是方向」（加仓／补仓／逢低买／抄底 → 看多）。表删掉后紧接着的「**引文必须连同方向词一起截**，不许只截前半句形态」与「先抑后扬」那条 blockquote（「不许因为它是形态就把这类真看多帖整条漏掉」）都是句子级的操作，不依赖这张表，原样保留。**判层规则一个字没减，只是不再重复一遍** |
| 2026-09-14 | §4.2 末尾「同一条帖既有复盘又有未来预测 → 只捕捉未来预测那部分」**下面补上那条解释**：帖内既有形态句「下周先抑后扬」，又有独立方向句「对于下周周线个人看涨」→ 产一条 `nweek`／看多，引文取方向句 —— 同一处该怎么挑句子，与「复盘/预测只取预测」是同一条道理，放在这里才读得通。写在 §6「两条放宽」表里原来也有一份，**这一份保留没删**（要改成移走，一句话的事）。`prompts.py` 未动，判断缓存指纹 `303ec21661325fe9` 不变、**不触发重判** |
| 2026-09-14 | 删掉 §4.1 起首那条举例 bullet「「明天会怎样，先看4000反击」→ 条件/观察句，不是对明天的预测」—— 上面那句「帖里出现「明天」二字，不代表这就是对明天的预测」已经把话说完了，举例是重复（且 `prompts.py` 里本来就没带这个例子，删了不动规则文本） |
| 2026-09-14 | **删掉 §4.1 的「整周无交易日 → 终点顺延」那条 bullet**（09-13 定案立的那条）—— 它讲的是**通用规则的一个实例，不是特例**：整周没有交易日时取该周最后一天（周日），它不是交易日，照「验证终点落在非交易日 → 顺延」走即可，正文不必单列。代码同批改：`market._last_td_of_week()` 去掉 `if days: … else next_td(…)` 那条分支，收敛成 `_settle(days[-1] if days else 该周周日)`。**结果一个字没变** —— 实测 26 组 `week`／`nweek` 改前改后逐格相同（2026 春节 02-16~02-23 全休，那一周的 `week` 仍落 02-24） |
| 2026-09-14 | **补回远端四处**（逐条裁定后落地）：① §2.3 补**终点价同源**那句 —— 「终点价也从这套 30 分钟线来：验证终点日**最后一根 30 分钟 K 线的收盘价**」（与 03§3.3 一致）；② §3.4 把「编造 vs 硬凑」的定性**改写成同等严重** —— 远端原文写「编造比无方向硬凑更严重」，定案是**两种错同样严重、都不允许**（`prompts.py` 同批改，否则规则文本与本文分叉）；③ §4.2 表下补回「各档的具体含义：`today` → 交易日收盘后；`week` → 当周最后一个交易日收盘后；`month` → 当月最后一个交易日收盘后；其余档位同理（对照 §4.1 的终点列）」；④ §2.1 补一条**取哪一根** —— 按代码实况写：**盘中**取**结束时刻严格晚于发帖时刻的第一根**的开盘价（整点发布**不回落**：`10:00:00` 取 `10:30` 那根），**其余**取**结束时刻不晚于发帖时刻的最后一根**的收盘价（`11:30:00`、`15:00:00` 取的仍是 `11:30`、`15:00` 那根本身）。远端原文那句用「开盘时间」叫根、与同句「bar 时间 = 收盘时间」自相矛盾，故按本地口径改写 |
| 2026-09-14 | **参考价口径反转**（推翻 09-13 那条「发帖前最后收完那根的收盘价」）—— 原则回到「**发帖时刻可获取的最新价格**」：报告、回测**盘中**（`9:30~11:30`、`13:00~15:00`）取**所处那根的开盘价**（10:20 发布 → `10:30` 那根）；**其余**（`11:30`、`15:00` 本身，以及午休、盘后、盘前、休市）取**上一根的收盘价**；**推送直接取现价**，不读 30 分钟线。09-13 那版有个洞：`09:30~09:59` 发的帖当天一根都还没「收完」，一律退到上一交易日收盘（实测 `09:30` 退到 3934.404，应为 3910.923）—— 事后不存在「那根 K 线还没出来」，**当天 9:30 那根是存在的**（标为 `10:00`，其开盘 ≈ 9:30 开盘价）。注记头随之改成 `盘中(30分钟线当根开盘)`／`上一根(30分钟线收盘)`，推送 `现价(推送时刻)`。§2.1 表与 30 分钟线结构两条、§2.2 调用方那句、§2.3 推送分岔跟着改；代码 `market._intraday_price()`／`snapshot_label()` 同改。**全库 1414 条信号里 135 条参考价变了**（多数是同一根开盘对上一根收盘的零头，`09:30~09:59` 那几条是大的）；`reports/` 里的报告与对比榜仍是旧口径产物，**待下一次全库重刷时对齐** |
| 2026-09-14 | **加回 `quote` 限长 ≤ 60 字**（推翻 09-13「显示端截、文件存全量」那条）—— §9 规则表加一行「超了截到 60 字内、**不许为凑长度改写**」，§10.1 强校验加一行「超长截到限内、**不丢行**、截断后仍须逐字可搜」。键定名 **`parse.quote_limit`**（归本文 §12），03§4.5／§8 与 06§6.2／§8 引用同一个键；原先挂在 `report` 一节的同名键删掉 |
| 2026-09-14 | §4.2 机械那半收窄为**本文只拦当场能定终点的三档**（`today`／`week`／`month`），其余档位归 03§3.6 ③ **机械兜底** —— 判据同一条（`pub` 晚于对应 `spec` 验证终点的收盘时间，**任何档位**），边界取等。其余档位的终点要摊开交易日历、跳过休市才算得出来，而本文是喂给模型的规则文本，拿不到那个日历；03 是事后打分，它有 |
| 2026-09-13 | **并入远端 `f10bdf8`／`555d06c`／`730bcdb` 的补漏**（远端三个 commit 打在重构前的 02 上，逐条审过）。采纳：§2.1 补 **30 分钟线结构**（每指数每日 8 根、bar 时间 = 收盘时间、无 `09:30` 那根）；§4.1 档位表补同义词（`week` 加「这周」、`monday`～`friday` 加「本周初」、`nmonday`～`nfriday` 加「下周初」、`long` 加「N日内」）、`today` 行改写为「必须**交易日的收盘前**发布」；**§4.2 分工句改写**（原写「过期不在这里判」，紧接着又列三档提取端拦截 —— 自相矛盾）；**§5.3 去重补「同一方向」**（原与 §7「方向相反 → 各一条」相抵）；§5.4「没给点位」→「没给点位，**给了方向预测**」；§10.1 补 `quote` 缺失或为空 → 丢掉 |
| 2026-09-13 | **未采纳的远端改动**（理由见各条）：① 四档快照性质（盘中／午休／盘后／盘前）—— 本地已定案「取发帖前最后收完那根的收盘价」，数值等价而更简；② §4.2 过期拦截扩为**全档位** —— 本地定案是三档 + 03 兜底，02 是模型，不该让它算交易日历顺延；③ `quote` **限长 ≤60** —— 本地定案是显示端截、文件存全量；④ 删 §6「**两条放宽**」表 —— 那是判层规则，删了「形态句 + 独立方向句」这类真看多帖就不再产；⑤「非交易日发布不视为信号」—— 与 03§3.4 冲突，见 03 一节 |
| 2026-09-13 | 定案：**「判断规则」＝本文**（§0 起首写明）—— 它是判定「哪句话算一条观点信号」＋「怎么给它定周期」的规则集，落成一份**规则文本**喂给模型（§11）、也进判断缓存的指纹（03§2.2）。此前这个词只在 03§2.2 与 §11 以「规则文本」的形式出现，没有一处说它指什么 |
| 2026-09-13 | §4 起首点明**「档」有两义** —— 本文的「档」＝周期档位（`spec`，说的是一段时间）；推送那个「档」＝时刻表上的一个点（06§5.1）。同一批删掉的术语表里 G 节挂着这一条 |
| 2026-09-13 | §6「周期词不救形态」一格把「无净方向承诺」改回**大白话**（「没有一句独立无条件的方向句」）—— 「净方向」是个没在正文定义过的自造词 |
| 2026-09-13 | 定案：**§2.1 的行情注记改取「发帖前最后收完的那根的收盘价」** —— 原写四种（盘中「所处那根的开盘价」／午休「11:30 收盘」／盘后「15:00 收盘」／盘前休市「上一交易日收盘」），现塌缩成一条：**结束时刻不晚于发帖时刻的最后一根 30 分钟线**；当天一根都还没收出来（盘前，或刚开盘那半小时）→ 上一交易日末根。注记头随之从四种改成两种（`10:00 收盘(发帖前最后收完的一根)`／`上一交易日收盘`）。**只用已经收出来的那根**：同一根 K 线的收盘与下一根的开盘是同一个时点，数值只差最后一笔的零头，但前者当场就取得到，后者要等下一根收出来。连带 03 的参考价、05 的成交价与逐档净值跟着变 —— 实测 05 的头号数字只动到小数点后第二位（累计 +44.86% → +44.88%，卡玛 7.56 → 7.58），04§3.2 交叉核对重跑后仍逐格相同 |
| 2026-09-13 | 待补 #4 **撤换** —— 原题是**「『星期几』落在已经过去的那一天」**（一度定为「先挂起、口径不动」）。**同日推翻**：§4.1 本来就分 `monday`～`friday`（本周）与 `nmonday`～`nfriday`（下周），周日说的「星期一」落 `nmonday` 是现成的档 —— 口径没有缺口，原题的定性不成立。重判实测 6/6 都判 `nmonday`，也不复现。「这条表述会被静默丢掉」是拿一个查不实的推断当成了既定事实。改记为**查实的那一件**：缓存把「不产」也钉死 |
| 2026-09-13 | **新增 §11 模型网关** —— 模型选哪个、要什么回来、调不通怎么重试、超时多久、全失败怎么办，五个键从原先没归口改成有归口的一节。定案：**模型名并进判断缓存的指纹**，换模型 = 旧的判断全部作废重判 |
| 2026-09-13 | **新增 §12 可调参数**（原 §11）—— `parse.*` 10 个键从仓库根的 `config.json` 读 |
| 2026-09-13 | 定案：**边界一律取等** —— §4.2 的「晚于收盘」：发帖正好 15:00:00 → 算晚于、按表拦；§5.4 的 ±10%：正好差 10% → 算带外、不产。两处边界都归严的那一侧 |
| 2026-09-13 | 定案：**±10% 的判断加脚本兜底** —— 模型判完，脚本按注记现值复算一道；与模型结论不符时**以脚本为准**（§10.1） |
| 2026-09-13 | §10.1 补一行强校验：**`d:` 的具体日期早于发帖日 → 丢掉**（§4.3 说「脚本再兜一道」，这道落在这里） |
| 2026-09-13 | 定案：**整周休市时终点顺延** —— `week`／`nweek` 指的那一周一个交易日都没有（春节那一周）时，「最后交易日」不存在，退到该周之后最近的一个交易日（§4.1） |
| 2026-09-13 | 定案：**跨年表述一律落 `long`** —— §4.1 的例子把硬编码的「2026年」换成不随年份陈旧的说法，「今年」「明年」都明列在 `long` 档 |
| 2026-09-13 | 定案：**§5.4 补全三分支** —— 核心预测句未点名指数时：给了点位且落在上证现值 **±10%** 内 → `idx` = 上证指数；给了点位但在带外 → **不产**；没给点位 → **默认上证指数**。同时定案**不靠点位反查归到别的指数**，不让 AI 判断「这其实是哪只指数」 |
| 2026-09-13 | 定案：**三条过期在提取端就拦** —— `today`／`week`／`month` 三档，发帖时间已晚于验证终点收盘的，这条表述只能是回顾。**写进规则由模型判，03 的代码检查保留作兜底**（03§3.6） |
| 2026-09-13 | 定案：**删掉 `nweek_first`** —— 「下周一」~「下周五」一律落 `nmonday`~`nfriday`。它有两个毛病：**整周休市时算不出终点**（下一周一个交易日都没有，返回空 —— 2026 春节前那一周即如此），且只覆盖「下周一」，周二~周五无档可落 |
| 2026-09-13 | 定案：**`long` 分界从 N>30 收到 N>20**，`tN` 合法上界同步收到 20 —— 20 个交易日约合一个月，超过就不算可验证的短期周期 |
| 2026-09-13 | 定案：**`t3`／`t4~t6`／`tN（N≥7）` 三行合并成一行 `tN`（N≤20）**。顺带修好两处不一致：「N日内」此前只在 N=4..6 认（`tN（N≥7）` 那行只写「N天后」）；`t3` 那句「只有明说数字 3 才落这一档」不再单列 |
| 2026-09-13 | 定案：**具体日期由模型直接写 `d:YYYY-MM-DD`** —— 博主给了具体日期（「8 月 20 日会涨」）就按那一天验证，不再折成 `tN`。此前写「引擎认，模型不写」（读起来像模型不许碰 `d:`），且删掉「坑三」之后这类表述无档可落 |
| 2026-09-13 | **§1 新增「先定位核心预测句」** —— 逐条提取前先按帖子结构定位核心预测句（一帖 0 个或多个），只有它才看三元组，其余语句（背景、铺垫、闲聊）不产出 |
| 2026-09-13 | §2 行情注记的用途 ① 改为「未点名指数时，对照上证现值判**合理范围**」（数值见 §5.4），不再是用点位反查对象 |
| 2026-09-13 | §4.1 补两条：**博主说什么周期就落什么档——不扩展、不缩短、不随意假设**；**「信号日」＝发帖自然日、「发帖后第 N 个交易日」一律从发帖日往后数**（周五盘后说「明天」→ 验证周一） |
| 2026-09-13 | **补回「改动记录」与「待补」两节** —— 2026-09-12 的提交把两节整个删了，01／03 都还有 |
| 2026-09-12 | 定案：**§5.4 改三分支**（未点名 → 按点位合理范围判／默认上证）；**不靠点位反查归到别的指数** |
| 2026-09-12 | 定案：**删 §4.3「三个坑」**，其中「『本周』已走完」并入 §4.2 的三条提取端拦截。**删掉「具体日期按自然日差落 `tN`」**（后由 2026-09-13 的 `d:` 定案补上） |
| 2026-09-12 | §4.1 spec 表精简：删 `nweek_first`／`t3`／`tN（N≥7）`，`t4~t6` 并入 `tN`（N≤20）；`long` 改为列举三情形、阈值 N>20 |
| 2026-09-12 | §1 对象表述改为「预测的是哪个板块或指数」；§5.2 映射表**加「代码」列**；§2 注记示例数值改占位符 |
| 2026-09-12 | 补入此前只在 `SKILL.md` 里的判读规则：先定位核心预测句、标题与正文同等重要、周期词不救形态、投资理念不成条、信号日定义与顺延、语义理解正反例 |
| 2026-09-12 | **立稿**（新结构下的 02，取代 `legacy/01-判断规则.md` 的判读部分） |

### 待补

| # | 待定的事 |
|:---|:---|
| 1 | ~~±10% 要不要随周期分档~~ —— **已结案**：不分档，短周期与长周期用同一个带（§5.4） |
| 2 | ~~「N日内」与 `tN` 的语义不完全对齐~~ —— **已结案**：保持现状，不为窗口类表述另设档（§4.1） |
| 3 | ~~§5.2 映射表的纳入判据~~ —— **已结案**：保持现状，不写判据，按表里列出的名单执行（§5.2） |
| 4 | **判断缓存把「不产」也钉死**（03§2.2）：一条帖第一次被判成「没读出信号」，缓存连这个**空结果**一起记死，之后**永远不再重判**。换模型会整批作废（指纹含模型名），**同一个模型下的判漏不会** —— 一旦某次没读出来，那条帖就再也不会被问第二次。要不要给缓存加个失效机制（按时间戳重判／只钉产出的行／手动清），是个待定的事。**实测**：香满衣 2026-09-13 那条周日帖缓存里是空的，重判 6 次 6 次都判出 `nmonday`；把这条从缓存里划掉重跑，信号正常进来 |
| 5 | ~~「整周无交易日」的兜底代码要不要删~~ —— **已结案**：它不属于特例，是「验证终点落在交易日」的一个实例（§4.1），正文那条 bullet 删掉。同日**顺延整个删除**（见改动记录），兜底改为「空集就退到该周周日／该月最后一天」，不由 `endpoint()` 挪，交给 03§3.6 ② 判「报错」（2026-09-14） |
| 6 | ~~**规则文本按本文整份重新生成**~~ —— **已结案**（2026-09-14）。拿本文当唯一来源把 `prompts.py` 整份重写了一遍，指纹 `603c3a6437ec77ef` → `9ff2cf78ce60f200`，八份判断缓存作废并已重判（见改动记录 02 同日那条）。连带项也已同步：`blogger/tests/fixture/state/parse_cache/aniu24W.json` 的 `rule` 改成 `9ff2cf78ce60f200` |


---

## 03 · 单博主报告

### 改动记录

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-14 | **修一处自己刚引进的 bug：`report/flow.py` 里 `verify` 指向错了模块**（跑回测时暴露，8 位全数报错）。本文第 17 行一直是 `from blogger.report import cache, render, stats, store, verify` —— 那个 `verify` 是**本文自己的事后验证**（`evaluate`／`SCORED`），而新加的抓完校验要调的是 `blogger.scrape.verify.verify`。同一个名字两个模块，`verify.verify(...)` 当场抛 `AttributeError`，八位一位都跑不成。改成 `from blogger.scrape import verify as scrape_verify` 并就地加注释点明两者之别。**在跑的重判不受影响** —— 那个进程早于这行代码启动，Python 载入的是旧模块，且八位的抓取校验本来就全过（rc=0、0 项硬失败），结果照用 |
| 2026-09-14 | **§6 本轮更新失败的名单补上「抓完校验硬失败」**（01§9 定案连带，见 04 同日同项）。名单原本只列抓取失败、解析失败、写报告失败，漏了「抓成了但校验过不去」这一种 —— 它同样是这一轮没跑成。代码侧：`report/flow.py` 的 `_scrape` 抓完就地调一次 `verify.verify(got, start, log=log)`，传的是**与抓取同一个**起点（起点不同覆盖检查判不了），硬失败即返回空串 —— 这位这次到此为止，只影响他自己 |
| 2026-09-14 | **补一处指纹缺口：`parse.runs` 没进判断缓存的指纹**（写代码时发现，用户裁定「先改文档再改代码」）。**症状**：把 `parse.runs` 由 1 改成 3 再跑，缓存**不会**作废 —— `rule_fingerprint()` 的 blob 只有 `ANNOTATION_SYSTEM_PROMPT` ＋ `RECONCILE_SYSTEM_PROMPT` ＋ `parse.model` 三样，`runs` 漏在外面。**实测**：`runs=1` 与 `runs=3` 的指纹**同为 `7a5ff7f4833806ee`**，`cache.load('云帆观市')` 照样命中 797 条。**后果**：改了「跑几遍」却静悄悄拿回旧参数下的结论 —— 与「只认规则文本、不认模型名」是同一个坑。`runs` 就是「一条帖跑几遍、再让模型在几遍之间定夺」，它同样改变读法，`cache.py` 自己那句「只认规则文本的话…会静悄悄地拿着上一版读法的结论往下跑」原样适用于它。**这是 03 待补 #4 那一族的第二个实例**（#4 记的是「同一个模型下的判漏被钉死」；这个是「换了参数仍拿旧参数的结论」）。**文档五处**：§2.2 表「规则文本一改」一格改「**规则文本／模型名／一条帖跑几遍 一改**」；§2.2 表下第三条 bullet 改「**规则文本、模型名、一条帖跑几遍 任一改**，缓存整批作废（比对这三样的校验码，对不上就当没判过）」；02§11 末句改「**模型名与「一条帖跑几遍」都进判断缓存的指纹**」；02§12 末条「换模型 = 整批重判」补上换跑几遍；06 那条 `--init` 的括注补上「或『一条帖跑几遍』」。**代码**：`cache.rule_fingerprint()` 的 blob 接上 `params.get("parse.runs", 1)`，模块 docstring 与函数 docstring 同改。**代价**：指纹 `7a5ff7f4833806ee` → **`603c3a6437ec77ef`**，缓存整批作废 —— 但这一轮本来就要清缓存重判，实际代价为零。**连带**：`tests/fixture/state/parse_cache/aniu24W.json` 的 `rule` 同步到 `603c3a6437ec77ef`，否则离线自测每次都要真调模型。**实测**：`runs=1` → `603c3a6437ec77ef`、`runs=3` → `cbaed237c0284008`，指纹跟着变；`python -m blogger.tests.drive` 跑通（`aniu24W：对得上　28｜-0.06｜46.4｜2.32｜-0.02`） |
| 2026-09-14 | **§4 三处收口**（承接下一条「是不是信号」拆分，用户裁定）。① **§4.4 点明是例外** —— §4 引子写「统计只用计分的信号」，而 §4.4 吃的恰是全部信号（计分＋不计分），不点明会被读成两句打架；引子末尾补一条行首标签「**§4.4 是例外** —— 那一节量的是样本形状，不进分数的行也在它的样本里」，§3.7 那张表「不计分（`long`）」一格同步补括注「（§4.4 的分布是例外）」。② **两张月表不再撞名** —— §4.2／§4.3 那列由「信号数」改**「计分信号」**，与 04 榜表同名。**改的是这一侧、不是 §4.4 那侧**：§3.6／§4.1 已把「信号」定义成计分＋不计分，§4.4 的时间分布表正是按这个定义数的，那列叫「信号数」才是正名；只算计分的四张表（三张分类表＋月度表）反倒该把样本说清楚。③ §4.2 引子与 §4.3 的措辞跟着实指。**代码同批**：`report/render.py:_table()` 的表头（分类表与月度表四张共用这一处），两个 docstring 与 `_table()`／`_distribution()` 写明哪张表吃哪份样本。**实测**：`compileall` 过 |
| 2026-09-14 | **修一处印错的键**：`report/flow.py` 第 ⑤ 步印的 `got['日历到']` 是**官方日历末日**（`2026-12-31`），却报成「行情到」。`fetch.refresh()` 返回的三个键里 `行情到` 才是数据补到哪天，`blogger/market/flow.py` 印的就是它，全仓喊「行情到」的六处只有这一处取错。改成 `got['行情到']` —— 报告链的日志从此与备料链一致，不会在盘前看着「行情到 2026-12-31」而当成数据已经补满 |
| 2026-09-14 | **端到端自测修好并跑通**。① `tests/drive.py` 一处**既有断裂**（HEAD 里就是坏的，不是本日引入）：`flow.judge_posts(BLOGGER, posts, 1, print)` 多传一个 `1` —— `judge_posts` 早先丢掉了 `runs` 形参（那条规矩移进 `config.json` 的 `parse.runs`），驱动没跟着改，一跑就 `TypeError`；改成三个实参。② fixture 缓存 `state/parse_cache/aniu24W.json` 的 `rule` 指纹**没跟着提示词同步**（`8923417a9c185691` → `7a5ff7f4833806ee`），`cache.load()` 按规矩「指纹对不上就当没有」，于是这一趟**每次都要真调模型**、没有 key 就直接退出；照 `tests/README.md` 那条把 `rule` 同步过来，`judged` 里 37 条原样留着。③ ⑤ 单列日志与末尾「产出的 N 行」跟上新口径，装全部行的变量由 `scored` 改名 `evaluated`（它装的从来不是计分行）。**实测**：`python -m blogger.tests.drive` 全程离线跑通 ②～⑦ 并出报告（头部「信号 28 条（计分 28）」、汇总块「信号总数：28（计分 28 / 不计分 0）／不是信号：无效-过时 0 / 待验证 0 / 报错 0」），04§3.2 交叉核对 `aniu24W：对得上　28｜-0.06｜46.4｜2.32｜-0.02` |
| 2026-09-14 | **「是不是信号」与「计不计分」拆成两件事**（用户定案）。① §3.6 新增一栏「**是信号？**」—— ①⑥ 填「是」，②③④⑤ 填「否」；引子写明「是信号的计入『信号总数』（§4.1），其中计分的才进各项指标」。② §4.1 汇总块由「信号总数 N ／ 另有：没算分 X 条（不计分／无效-过时／待验证／报错）／ 计分信号 M 条」压成「**信号总数：N（计分 M / 不计分 K）／ 不是信号：无效-过时 X / 待验证 X / 报错 X**」—— **那三档不再并进信号总数**。③ §4.4 集中度警告的样本由「全部行」收成「**信号**」（计分与不计分都算），措辞由「不分指数、不分状态」改「计分与不计分都算、不分指数；不是信号的三档不在内」。④ §3.7 由「一段话＋三条 bullet」改成两张表（哪一档进哪／文件与报出的规矩）；§4.5「列全部信号」改「**每一行都列**」—— **行照留**，不是信号的三档照常出现在逐条表里，只是不进「信号总数」。⑤ §3.8 的标题与正文由「观点信号」改「**行**」。**射程只到 03/04 的统计**（用户定案）：05/06 讲的是「从信号文件读到的条目」，推送卡上本来就有终点未到的那些（06§5.7 只丢三类），不动。**代码同批同步**：`stats.signals()` 新增、`tally()` 加 `signals` 键（计分 ＋ 不计分，`total` 仍是全部行）、`render._head()` 与 `_summary()` 改印新形态、`_distribution()` 改吃 `stats.signals()`、`_notes()` 的「单列的信号」改「单列的」、`tests/invariant.py` 的读数正则由「计分信号：M 条」改「信号总数：N（计分 M / 不计分 K）」；顺手删掉 `compare_all/pool.py` 里一个没人读的死键 `signals`（与新 `tally()` 的 `signals` 同名不同义，留着是坑）。**实测**（`云帆观市`）：`total 337 → signals 331（计分 322 / 不计分 9）`，不是信号 6（无效-过时 0 / 待验证 5 / 报错 1）；集中度样本由 337 收成 331 |
| 2026-09-14 | **旧产物删掉** —— `reports/per_blogger/` 八份与 `reports/博主对比.md`（全是 09-13 的：既是旧参考价，汇总块也是旧格式）**从库里删除**，等下一次全库重刷时重新生成。同批修了 `tests/invariant.py` 的一处**误报**：报告文件不在时原先落进「只有一边有那五个数 → 对不上」，现在先一步判「报告还没出」、不算失败 —— invariant 断言的是「两边都有那五个数时必须逐格相同」，产物不存在不是它的断言对象；报告刚删那阵子八位全红，正是把「还没跑」说成了「算错了」 |
| 2026-09-14 | **与远端 `730bcdb` 的 03 逐条对过**（33 处差异全过审）。**采纳 4 条**：① §3 依赖表 `note` 行补回「**行情覆盖**」—— `_state()` 判 ④⑤ 确实要它；② §3.8「解析阶段」那行的括号补上「**02§4.2 拦截表挡下的**」，此前拦截表挡下的帖没有任何清单认领；③ 维持 §4.5 引文截断的简写；④ 维持 §4.1 的三行结构（不写 `N = M + X`）。**其余 19 条维持本地**（五态／官方日历／不顺延／待验证／计分信号判定线／信息比率／`per_blogger`／无边距覆盖警告／由缓存派生／行情来源归 01／无「跑几遍」行／七样不落盘／覆盖区间／批间隔／算不出跨度不进两档表／边界取等／印出来的位数／全部信号／一位跑不成不牵连／`--no-market`／§8 可调参数），**删 1 条**（§4.6「只有一项时不并列」）。**另定两件新事**：① 状态机**加第 ⑤ 步**「水位到了、这个指数在终点那天却没有行情 → **报错**」，原 ⑤ 计分顺移为 ⑥，④ 的判据同时由 `ep > 今天` 改成 `ep > 行情水位` —— 与「砍掉日线」同批（缘起见 01 同日那条）；② **② 改为兜底** —— 02§4.2 第一行已在提取端拦掉「终点落在非交易日」的帖，② 正常跑进不来，正文写明留着只防上游漏拦。§2.1、§3.3 去掉日线两处，§2.1 行情那句「补到**运行当日**」一并改成「补到**该到哪天**」（行情补到哪天归 01§10.4 的 `due_day()`，运行当日只是它的其中一种情形 —— 与 06§5.3 同一口径）。**实测**：全库 1246 条计分，状态分布不变；新第 ⑤ 步 0 命中（3 条报错全是 ②） |
| 2026-09-14 | **通读校对**（见 01 同日那条的缘起）。本份十处：① §2.1「覆盖区间 → 运行当日」改成「→ 该到哪天（01§10.4）」—— 行情补到哪天不是「运行当日」，盘前与盘后各是一根；② §2.2 表补一行「批与批之间的间隔｜0.5 秒」—— `parse.batch_pause` 此前只在 06 与 `config.json` 里出现过，它自己的归属文档反而没写；③ §3.3「取算术平均」→「取平均」、「两个展示值是两指数各自的均值」→「两指数**对应值的平均**」（`ref` 与 `epc` 各是一次平均，不是「各自的均值」）；④ §3.4「日历含未来一年」→「含未来」；⑤ §3.5 那条 blockquote 的「`ret` 走 §3.3 的 mean-of-returns」改成中文实指；⑥ §3.7「（`today`／`week`／`month` 三档，见 02§4.2）」改成「（见 02§4.2 的**整张拦截表**）」—— §4.2 表现在是四行，spec 拦截只是后三行，数成「三档」既数错也说窄了；⑦ §4.1 补一行「**印出来的位数**：正确率一位小数，其余两位」—— 04§3.2 一直写着「位数照报告取」，而本份没这条规则，两边对不上；⑧ §4.4 补一句「**算的是全部信号** —— 不分指数、不分状态」—— §4 起首写着「统计只用计分的信号」，§4.4 实际吃的是全部行，不写明会被读成只算计分；⑨ §4.5「`note` 正常填 `—`；盘中「今天」填 `日内`」改成实指「计分填 `—`；计分且是盘中「今天」的填 `日内`（§3.6 那个附加标记）」；⑩ §3.8／§6／§8 删「为什么」三处，另 §6 表的保存机制由「并入，旧的不动」改成「**由缓存重新派生 —— 整份写回**」（§2.2 早已定案由缓存派生，§6 还在说并入），§8 的「两档边界只有这一处（04§3.4）」改成「定义在 §3.4，04§3.4 引用它」。**头部字段定名**：「评估时间」改「数据截止」—— `blogger/compare_all/render.py` 与 04§5／05§6 都写「数据截止」，只有 `blogger/report/render.py` 写「评估时间」，四份里三票对一票，取「数据截止」。**代码同批同步**：`report/render.py:_head()` 的 docstring 与印出来的那一行都由「评估时间」改「数据截止」 |
| 2026-09-14 | **「一条帖跑几遍」移归 02**（来龙去脉见 02 节）—— §2.2 那张表只管批与缓存，删掉「一条帖跑几遍｜1 遍（见 02§10.2）」那一行；§7 两行示例命令去掉 `[--runs 1]`、底下 `--runs N` 那条注记一并删掉。定案：**值只在 `config.json` 的 `parse.runs` 里，命令行不开这个口子**（与 05§8 同一条规矩） |
| 2026-09-14 | **验证终点不再顺延**（02 那条的 03 侧，来龙去脉见 02 节「删除顺延机制」）。§3.2 引子「终点落在非交易日 → 顺延到之后最近的一个交易日」→「**终点就是博主说的那一天，不顺延** —— 落在非交易日就是这条不成立（§3.6 ②）」；§3.6 ② 行由 `today` 专用改为通用判据「验证终点落不到交易日 —— 说的那天没开市（`spec=today` 而发帖日不是交易日，即其一例）」；§3.3 表第 3 行「终点顺延、基准日、跨度全依赖它」→「终点**是不是交易日**、基准日、跨度全依赖它」（日历那一栏本来还管着「这天开不开市」）。判定**次序不变**：② 报错 仍在 ③ 无效-过时 之前 —— 一条终点落非交易日的 `today` 帖（发帖日＝终点日）过去被 ③ 判成「无效-过时」，现在归 ② 报错 |
| 2026-09-14 | 引文截断长度改引 **`parse.quote_limit`**（02§12）—— `report.quote_limit` 这个键删掉，正文里不再有第二个引文长度。§4.5、§8 两处引用跟着改，§8 补一条：引文在 02 那边就截好了，这里只是渲染时再兜一道 |
| 2026-09-14 | §2.1 纠一处**描述错位**：原写「日线要覆盖到今天；30 分钟线要覆盖到**最后一条信号的验证终点**」，把**打分的要求**（终点要够才计分）写成了**抓取的目标** —— 两者的目标日其实同一个，都是 `fetch.due_day()`（01§10.4）。改为「**覆盖区间**：日线、30 分钟线 → 运行当日」，下一行另说终点接不上的那些记「待验证」 |
| 2026-09-13 | **代码向文档对齐**（口径改动先改文档、再同步代码）。落进代码的是：`flow.py` ⑤ 由「清库」改「单列」—— 不再写回缓存与信号文件，`_prune()` 与 `verify.is_stale()` 一并删除；⑦ 逐条打印不计分／无效-过时／待验证／报错；`stats.accuracy()` 的分母改为计分信号数（`summarize()` 里与 `n` 重复的 `denom` 随之去掉）；`tally()` 加「无效-过时」一档；`render._head()` 加评估时间／帖子／信号三个字段；`sample_short()` 改数计分信号（本文与 04 两个调用点）；`verify._ret()` 双创改**逐指数各算收益率再平均**，双创成员收口成 `market.COMBO`；`parse/prompts.py` 补 02 的六处规则文本 |
| 2026-09-13 | §4.1 汇总块首行改「**没算分** X 条（不计分 X / 无效-过时 X / 待验证 X / 报错 X）」—— 原写「不计分 X 条（…）」，外层 X 是四档合计，而 `long` 那一档的状态名恰好也叫「不计分」，读起来像自相矛盾。§8 的 `sample.signals` 说明由「观点信号下限」改「**计分信号下限**」，与 §5 判据对齐（键名不动） |
| 2026-09-13 | **并入远端 `730bcdb` 的改动**（逐条审过）。采纳：① **§3.3 双创改 mean-of-returns** —— 两个指数各算各的收益率再平均（`ref`／`epc` 仍是两指数均值，§3.5 加例外注）。原写「各自的终点价取算术平均」，对 50/50 组合会被两指数起点的高低带偏；② **§3.7 由「清库」改为「不删行」** —— 无效-过时／待验证／报错的行**照常留在文件里**，也照常出现在逐条汇总表，只是不进统计。**留着才看得见「这条为什么没算分」，可人工复核**；连带删掉 §2.2「清库也要清缓存」（行不再删，就没有缓存要清），§1 一图流 ⑤「清库」改「单列」、⑦「过滤清单」改「清单」，§3.7／§3.8／§4 各处引用与 04§3.3 跟着改；③ **§4 加报告头部字段**（评估时间 = 最新交易日／帖子总数）。另：§4.1 **正确率分母含得分为 0**（原算「平」、不计入分母）；§5 判定线由「观点信号 > 10 条」补明为**计分信号 > 10 条**；§3.8 删「编号指错的」（`post_id` 由系统补、模型不写编号，不存在这一项） |
| 2026-09-13 | **未采纳的远端改动**：① 状态**收敛为「计分／不计分」两种** —— 本地保留细分名（不计分／无效-过时／待验证／报错），逐条表里一眼看得出是过时、算不出还是报错；② 报告文件名加 `_direction`（本地 09-12 已定案去掉）；③ 「信息比率」改回「夏普」（本地 09-12 已定案改名 —— 与回测量净值曲线的夏普不是一回事）；④ §4.4 加回「覆盖 < 3 个月」（本地 09-12 已定案删除，与 §5 跨度线重复）；⑤ **「非交易日发布的帖不视为信号」** —— 本地维持 §3.4「发帖日不是交易日 → 往前取最近一个交易日当基准」，「报错」状态随之保留 |
| 2026-09-13 | 定案：**产物挪进 `reports/per_blogger/`** —— `reports/` 从此是产物根，03 与 04 的产物都在它下面各占一层（04 见本文件「04 · 全博主对比」一节）。正文里的 `reports/<博主名>.md` 一律改口径 |
| 2026-09-13 | §4.6 定案：分档／分指数**不再评「最强／最弱」**，改为**逐个列出各档各指数的平均分**（实测差 0.01 也报成一强一弱，误导）。连带删去「只有一档／一只时不并列最强最弱」那条 —— 不评强弱就没有并列问题 |
| 2026-09-13 | 定案：**交易日历改用官方日历**（含未来一年，01§10.3），不再以「有上证行情的那天」为准。§3 前提 3、§3.4 两处跟着改。连带后果：**验证终点现在算得出未来的了** —— 原先 `t1`（明天）、`nweek`（下周）这类终点全落在行情末日之后、一律算不出，卡上写不出日期；换日历后正常。§3.4 末条「算不出跨度」也只剩「终点超过日历末日」一种 |
| 2026-09-13 | 待补 #2 **结案** —— 06 立稿时定下推送也按**交易日跨度**分档（06§2），与本文 §3.4、04§3.4 同一口径；旧栈的「卡片词判层」不带到新栈 |
| 2026-09-13 | §2.1 的**行情来源整块挪进 01§10**（新增的一节）—— 两组数据从哪来、怎么降级重试、**抓完怎么核对**，都归 01。这里只留 03 自己的事：补到哪天算够、覆盖不到怎么办。此前来源写在这里，与 01 各说各话 |
| 2026-09-13 | §4.6 补：**只有一档／一只时不并列最强最弱**（只有一项，「最强」与「最弱」是同一个值）；**只有一条计分信号时不并列「命中最狠」与「失误最大」** |
| 2026-09-13 | §8 末的「抓取的参数见 01§11」改指 **01§12**（01 新插了一节 §10 备料：行情） |
| 2026-09-13 | **新增 §8 可调参数** —— `sample.*`／`warn.*`／`report.*` 共 7 个键从仓库根的 `config.json` 读；并写明 `sample`／`warn`／`report.bucket_span` **与 04 共用同一份** |
| 2026-09-13 | §3.4／§4.2 **补上「算不出跨度的不进两档表」**，并写清两种来源（`long`；以及发帖日或验证终点不在交易日历上的计分行）。原先只把「跨度为 `None`」挂在 `long` 上 |
| 2026-09-13 | §6 **补上一句「一位跑不成不牵连其余的」** —— 抓取／解析／写报告失败都只记下这一位、接着跑下一位，绝不拿旧产物顶上（与 04§2 同一套话） |
| 2026-09-13 | §4.5 示例勘正：`idx` 由 `上证` 改为 **`上证指数`**，与同节「列名就是字段名」自洽 |
| 2026-09-13 | §1 一图流**补上第 ⑦ 步「过滤清单」** —— 此前只画到 ⑥，而流程与 §3.8 都有第 ⑦ 步 |
| 2026-09-13 | §2.3 措辞收紧：原写「什么都不落盘」，与「报告要写成 `reports/<博主名>.md`」相抵 —— 改成**只有 §3 那七样不落盘** |
| 2026-09-13 | §7 补上漏写的两个参数：`--runs`、`--no-market` |
| 2026-09-13 | 待补 #4 结案（观察要点**按规则拼，不交给模型**）、#6 结案（04 门槛见 04§4，且 §5 的标注线与 04§4.1 是同一个键） |
| 2026-09-13 | §3.6 跟着 02§4.2 同步：① 的 `long` 判据 `N>30` → **`N>20`**；③ 补齐**边界取等**（发帖正好 15:00:00 算「晚于」→ 无效-过时）；§3.7 的出处引用由已删的「02§4.3 坑一」改指 **02§4.2** |
| 2026-09-12 | **立稿**（新结构下的 03，取自 `SKILL.md` §4–§8） |
| 2026-09-12 | 定案：**事后验证的结果什么都不落盘** —— 结构化数据文件**永远 6 键**（02 的产出）。参考价／终点价／跨度／收益率／得分／状态全是「结构化数据 + 行情」的纯函数，**每次重跑、算完就扔** |
| 2026-09-12 | 定案：**事后验证不调模型** —— 纯脚本，同一条数据跑几次结果都一样 |
| 2026-09-12 | 定案：**旧博主更新时全量重算验证与统计** —— 不落盘，重算成本几乎为零 |
| 2026-09-12 | 定案：**产物改名** —— `reports/<博主名>.md`（去 `_direction`）；`data/signals/`（原 `data/direction_signals/`）；脚本名一律不带 direction |
| 2026-09-12 | 定案：**拆成两个命令** —— `report`（一位，入库或更新）／`report_all`（全库） |
| 2026-09-12 | 定案：**参数是「一个定位」** —— 博主名或帖子链接。名字查不到 → **报错**；链接查不到 → **入库**。**新博主只能靠链接引进** |
| 2026-09-12 | 定案：**§5 的门槛只标注、不挡流程** —— 每位博主都出报告，不达标的在头部写明差在哪。**不能跳过**：跳过等于他的帖从没被抓过、解析过 |
| 2026-09-12 | 定案：**`report_all` 的名单取 `data/posts/`**（所有抓过的博主），不取已有信号文件的博主 |
| 2026-09-12 | 定案：**§4.4 删掉「覆盖不足 3 个月」** —— 与 §5 的「跨度 ≥6 个月」是同一件事的两个阈值，只留分布类警告 |
| 2026-09-12 | **新增「交易日跨度」** —— 发帖日到验证终点的交易日数，用来分超短期／波段两档 |
| 2026-09-12 | 定案：**`cat` 不再由 02 产出**，改由本文从 `spec` ＋ `pub` ＋ 行情覆盖**现算**（§3.6 的五档状态） |
| 2026-09-12 | 定案：**逐条汇总表去掉「强度」列**（`s` 字段已删） |
| 2026-09-12 | 定案：**「夏普」改称「信息比率」** —— 它量的是观点本身的稳定性，与回测里量净值曲线的夏普不是一回事，名字必须分开 |
| 2026-09-12 | **§3 补「6 键够不够」** —— 参考价／终点／终点价／跨度只吃 `pub`／`spec`／`idx` 三键，逐项列出；并写明三个前提：`pub` 精确到分钟、行情覆盖两端、交易日历唯一 |
| 2026-09-12 | 定案：**起始时间固定**，只解析该日期之后发布的帖子 |
| 2026-09-12 | 定案：章节改称 **`begin_date`**，一个参数。**§2.1 写明越界帖归 01 管**，主文件拿到手天然在区间内，本文不再筛一道（原先的实现里这里多筛了一道，撤掉） |
| 2026-09-12 | §2.1 **补「行情从哪来」**：日线走东财（算交易日历）、30 分钟线**必须走新浪**（算参考价与终点价，东财按 TLS 指纹拦分钟线）。此前只写「两组都要」，没有一份文档认领来源 |
| 2026-09-12 | 新增 **§3.8**：被过滤掉的观点信号（解析阶段丢弃／清库删除／注记取不到）**逐条打印在命令行**，不写进报告 |
| 2026-09-12 | §2.2 补**判断缓存的形态**（待补 #8 结案）：按 `post_id` 记、规则文本一改整批作废、**信号文件由缓存派生**、清库要连缓存一起清 |
| 2026-09-12 | §4.5 定案：`pub` 与 `ep` **写完整日期** `YYYY-MM-DD`（此前示例写的是 `01-07`，与字段值不一致） |
| 2026-09-12 | §7 命令路径改为新包的 `blogger.report.report` / `report_all`，并写明 `--begin_date` 只对新博主有效 |
| 2026-09-12 | 定案：**批的参数（15 帖／3 万字／单帖 4000 字／从新到旧）落在本文**，推送侧必须与之一致。**「一条帖跑几遍、怎么定夺」移归 02**（02§10.2） |
| 2026-09-12 | **新增 §3.7「清库」** —— 事后验证判完、统计之前，把 `note` 为「无效-过时」的行**从文件里直接删掉**。删的是行，文件仍是 6 键；「无效-过时」从此不进统计、也不进逐条汇总表 |
| 2026-09-12 | 定案：**现算的七样各给一个固定英文字段名** —— `ref`／`ep`／`epc`／`span`／`ret`／`score`／`note`（§3）。逐条汇总表的列名直接用字段名 |
| 2026-09-12 | 定案：**逐条汇总表的 `spec` 列显示字段值本身**（`t1`／`week`／`nweek`…），不做中文映射 —— 档位含义看 02§4.1 |

### 待补

| # | 待定的事 |
|:---|:---|
| 1 | ~~「未来几天」落 `t3` 还是 `t5`~~ —— **已结案**：落 `t5`（见 02§4.1）。`t3` 只留给明说数字 3 的 |
| 2 | ~~两档判据有两套、会分叉~~ —— **已结案**：06 立稿时定下**推送也用交易日跨度**（06§2），与本文 §3.4、04§3.4 同一口径。旧栈那套「拿卡片上的周期词判层」不带到新栈 |
| 3 | **`long` 之外还有没有别的「不计分」**：现行口径只有 `long` 一条进 §3.6 的第 ① 步 |
| 4 | ~~报告里的「观察要点」由谁写~~ —— **已结案**：**按规则拼出来，不交给模型**。2026-09-13 定案：03 与 04 的表格、结论、警告清单一律脚本拼；只有 04 报告末尾另有一段模型写的「总结与分析」（见 04§5） |
| 5 | **脚本名待确认**：`report` ／ `report_all` 是我起的名；提取脚本相应从 `extract_signals_direction.py` 改成 `extract_signals.py` |
| 6 | ~~04 榜单门槛~~ —— **已结案**：04 的两道门槛见 04§4。本文 §5 的标注线与 04§4.1 是**同一条线、同一个键**（04§8） |
| 7 | **「报错」要不要跟着「无效-过时」一起删**：§3.7 现在只删「无效-过时」。两者都是**验证不了的信号**（一个是发帖太晚，一个是算不出：终点落不到交易日、或该指数那天没有行情 —— §3.6 ②⑤）—— 只删一个，逐条表里就永远挂着另一批；要删就一起删，要留就一起留。**实测全库报错 3 条**（2026-09-14 加第 ⑤ 步后重数），所以这条还没被逼到 |
| 8 | ~~缓存长什么样~~ —— **已结案**：一位博主一份判断缓存、按 `post_id` 记、规则文本一改整批作废、信号文件由它派生（见 §2.2） |


---

## 04 · 全博主对比

### 改动记录

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-14 | **§2 本轮更新失败的名单补上「抓完校验硬失败（01§9）」**（与 03 同日同项）。一句话的补漏：抓成了但校验过不去，与抓取失败一样算这位这一轮没跑成，**绝不拿他上一次的旧数据顶上**（§2 原有那句已经这么写，只是名单里少列了这一种） |
| 2026-09-14 | **§6 的样本措辞改成实指**（承接下一条，用户裁定）。那条原写「单博主报告那一节算的是该博主的**全部信号**（计分与不计分都算）」—— 「全部信号」是个绕圈子的说法，且「全部」二字读起来像把不是信号的三档也算进去了。改成「该博主的**信号**（计分与不计分两档都算，03§4.4）」—— 「信号」在 03§3.6／§4.1 已定死是计分＋不计分两档，不必再加「全部」；「两档」二字同时点明不是信号的三档确实不在内。`compare_all/boards.py:warnings()` 的 docstring 同改 |
| 2026-09-14 | **跟着 03 的「是不是信号」拆分收口**（03 同日那条）。§3.3 标题由「不进榜的信号」改「**不进榜的**」，并写明「不计分**是信号**，另三档（无效-过时／待验证／报错）**不是**（03§3.6）」；§2 末「没进统计的**观点信号**不在这里重复报」改「没进统计的**行**」；§5.2 那条「与单博主报告里那一节（算该博主的全部信号）」改成明写样本 ——「单博主报告那一节算的是该博主的**全部信号**（计分与不计分都算，03§4.4）」。**榜上的数字一个不动** —— 榜只吃计分信号（§3.1），本次只动「信号」这个词的用法 |
| 2026-09-14 | **榜放开：不再「只考察上证」，改成吃全部计分信号**（推翻 09-13「三张榜全部限定上证」那条）。§3.1 由「榜单只考察上证指数」整节改写为「**榜不分指数**」：榜上一切数字都算这位博主的**全部计分信号**，八个指数混在一张榜里排；得分本身是百分数（方向 × 收益率 × 100，03§3.5），拿它横着比与说的哪个指数无关；**每张榜的标题都不带指数限定**；分指数的事仍在报告里做（03§4.2）。**条数一个不动**（用户定案「条数不动，只改口径」）：参与对比仍 跨度 ≥6 月 且 计分信号 >10 条，总榜仍 ≥30 条且均分 >0，分榜／方向榜仍 ≥10 条且均分 >0.1。跟着收口：§1 一图流删掉「只留预测对象是「上证指数」的」那一支；§3.2 那句改成对 03§4.1 的「汇总指标」逐格比对，原先「两边都没有那一行 = 一致」「不许拿一行全是 0 的顶上去比」整段删掉（榜上不再按上证筛，「一条上证计分信号都没有」这种情形不存在了）；§4.1／§4.2／§5.2／§6 里的「上证」二字全部去掉。**范围只放开 04**：05 回测与 06 推送照旧只吃上证，两处都把「上证」写死在正文里、不开配置键（用户定案「0506 都写死为 idx=上证」）。**`compare.index` 键从 `config.json` 删掉** —— 04 不再有「预测对象」这个维度：04§8 删掉那一行、06§9 删掉那一行、06§9 底下那条「只看哪个指数与 04§3.1 是同一个键」删掉，`compare._` 补一句「榜单不分指数，没有指数键」。**顺带回退本日「通读校对」那条 ①**：`config.json` 的 `sample._` 与 §8 的 `sample.signals` 由「全指数的计分信号」改回「计分信号」—— 榜放开后「全指数」是废话。**引用同步**：05§0「与 03§4、04§3.1、06§2 同一口径」改成「与 06§0、06§2 同一口径」（04 已不在这条线上），06§0「与 03§4 的榜、04§3.1 同一口径」改成「与 05§0 同一口径」。**代码同批同步**：① `compare_all/pool.py` 删掉 `index()` 与 `params` 的 import，`collect()` 不再产 `board` 键（`scored` 就是榜上那份样本），`__all__` 跟着改；② `boards.py` 五处 `p["board"]` 全改 `p["scored"]`（总榜／两档／两方向／`warnings()`），模块 docstring 的「三张榜全部只考察上证指数」改成「榜不分指数」；③ `render.py` 头部资格线由「{idx}计分信号 ≥ N 条」改「计分信号 ≥ N 条」、榜标题由「## {title} · 只考察 {idx}」改「## {title}」、警告节那句样本说明去掉「上证」；④ `backtest/flow.py` 定常量 `IDX = "上证指数"`（注释写明 05§0 是写死的）、`_load()` 改用它，`params` 的 import 删掉；⑤ `push/conf.py` 定常量 `IDX = "上证指数"`、`load()` 的 `"index"` 改用它，**顺带修一处静默错误**：`quote_limit` 原先读 `report.quote_limit`（这个键不存在，靠 `.get` 默认值 60 恰好等于真值），改成 `parse.quote_limit`（02§12），`push/card.py:_clip()` 的 docstring 同步；⑥ `blogger/tests/invariant.py` 的 `report_row()` 由「读『按预测对象』表里上证那一行」改成「读『汇总』块那五个数」（`stats.summarize(p["scored"])`），`check()` 去掉 `idx`，措辞由「那一行」改「那五个数」。**实测**：`compileall` 过；离线跑 `pool.collect` + `boards.all_boards` + `render.head/board_section` —— 榜标题无指数后缀、资格线不带指数、三人样本的五个数按全指数算；`python -m blogger.tests.invariant` 8 位里 5 位逐格相同，另 3 位是旧产物所致（见待补 #1） |
| 2026-09-14 | **通读校对**（见 01 同日那条的缘起）。本份五处：① §4.1「计分信号 > 10 条」改成「**全指数的**计分信号 > 10 条」—— 紧邻的 §3.1 说「榜上一切只算上证」，这里数的是全库全部计分信号，不写明必被读反（同一张表的总榜那行本来就写着「上证计分信号 ≥ 30 条」，两行体例也不一致）；② §5.1「**加粗的那一列 = 平均分**」改成「**平均分那一列的数加粗**」—— 示例表头里没有任何一列是加粗的，读不出指的是哪一列；③ §5.1「未达上榜资格的行，排名列写 `—`，末列写原因」改成「行尾另加一列写明差在哪」—— 列定义里末列是「看空」，而方向榜不含那两列，「末列」指代不明；④ §3.2「位数照报告取（正确率一位小数，其余两位）」改成「位数照 03§4.1 取」（该规则已在 03 补上）；⑤ §6 删掉「『覆盖不足 3 个月』不在这里判 —— 它是 §4.1『跨度 ≥ 6 个月』的另一面」—— 那条判据 2026-09-12 已从 §4.4 删掉，这里成了指向不存在条目的残留。§8 的 `sample.signals` 同步补「全指数」 |
| 2026-09-13 | **代码向文档对齐**：资格线文字由「观点信号 > 10 条」改「**计分信号 > 10 条**」（§4.1），参与对比的判据跟着改数计分信号（与 03§5 同一条线、同一个 `sample_short()`）。§8 的 `sample.signals` 说明由「观点信号下限」改「**计分信号下限**」（键名不动） |
| 2026-09-13 | §3.3 跟着 03§3.7 改 —— 原写「「无效-过时」在 03 的清库那一步就被删了，文件里根本没有」，现改为「这些行都还在信号文件里（03§3.7 不删行），只是排不出名次」；节名由「不进榜的三种」改「**不进榜的信号**」 |
| 2026-09-13 | 定案：**产物从 `compare/博主对比.md` 挪到 `reports/博主对比.md`** —— `compare/` 这个顶层文件夹取消，04 与 03 的产物同落 `reports/` 之下（03 的在 `per_blogger/`，见本文件「03 · 单博主报告」一节）。**「两份产物分开」的定性不变**，只是从「两个顶层文件夹」改成「同一根下的两层」，一份都不混 |
| 2026-09-13 | §2 定案：**全库更新是一个独立的动作，不只服务于对比** —— 05 回测也调它（05§8），要用这一步的都引用本文，不另写一套说法。§1 一图流 ① 跟着标注。并补两条操作代价：**别重复跑**（对比内部已跑过，再手动跑 `report_all` 等于全库在网线上走两遍）；**这一步联网抓帖、省不掉** —— 只改报告措辞／排版也得走这一遍，想让改动立刻生效就得重刷全库 |
| 2026-09-13 | §3.2 补两条：**两边都没有那一行 = 一致，不是错了**（一条上证计分信号都没有的博主，报告里没有这一行、榜上也排不出 —— 交叉核对脚本原先拿一行全是 0 的顶上去比，把「没有可比的东西」报成「对不上」，养基阳哥、香满衣两位假警）；**「逐格相同」比的是纸面上的那一格**，位数照报告取（正确率一位小数）—— 原先脚本按两位比，把 `47.1` 与 `47.06` 的**格式差**报成了「对不上」 |
| 2026-09-13 | **立稿**（新结构下的 04，取自旧栈的全博主对比设计）。定案：**全博主对比另立文件夹 `compare/`** —— 与单博主报告 `reports/` 分开，一份都不混 |
| 2026-09-13 | 定案：**首步骤是全库更新** —— 对比自己不抓帖、不解析，只读 03 的产物 |
| 2026-09-13 | 定案：**没跑成的博主不带进对比**，单列「本轮更新失败」。**不拿旧数据顶上** |
| 2026-09-13 | ~~定案：榜单只统计预测对象是「上证指数」的信号~~ —— **同日细化**：只有**榜单**限定上证，**报告**是全指数（见本表末行） |
| 2026-09-13 | 定案：**两道资格** —— 参与对比＝03§5 的标注线（跨度 ≥6 月 且 信号 >10 条）；上榜＝总榜 30 条且均分 >0／分榜 10 条且均分 >0.1 |
| 2026-09-13 | 定案：**三张榜** —— 总榜／两档分榜／方向榜，每张只取前 20。~~排序键各不相同~~ —— **同日改为一律按平均分** |
| 2026-09-13 | 定案：**两档边界维持不变** —— 跨度 ≤1 超短期、**≥2 波段**；补写 **`long` 不进任何一档**（它不计分，且跨度为 `None`） |
| 2026-09-13 | 定案：**报告 ①～⑥ 全由脚本拼**（数字、表格、警告清单一个字都不经模型），**只有末尾第 ⑦ 段「总结与分析」由模型写** |
| 2026-09-13 | 定案：**本文每一条判据参数化**，落在仓库根的 `config.json`（§8）；同文件里抓取／解析／单博主报告各占一节 |
| 2026-09-13 | 定案：**三张榜一律按平均分降序**（原先总榜按信息比率）—— **信息比率只列不排** |
| 2026-09-13 | 定案：**命令是 `python -m blogger.compare_all`**；产物 `compare/博主对比.md` |
| 2026-09-13 | 定案：**§4.1 的资格线与 03§5 共用配置里的同一个键**（`sample.*`），不各配一份 |
| 2026-09-13 | 定案：**报告不留档**，每次覆盖同一份（与 `reports/<博主名>.md` 一致） |
| 2026-09-13 | 定案：**第 ⑦ 段「总结与分析」只讲三件事** —— 谁强谁弱、哪些名次因样本问题不可信、与上一版的明显变化 |
| 2026-09-13 | 定案：**三张榜全部限定上证**，每张榜标题写明「只考察 上证指数」；全指数的统计只活在 03 的报告里 |
| 2026-09-13 | §8 勘正：**`sample` 与 `warn` 落在配置的顶层**（与 03 共用同一份键），本文自己的键在 `compare` 一节；表里补上 §3.4 的 `report.bucket_span` 与 §6 的 `warn.*` |
| 2026-09-13 | §5② 勘正：**够不上 §4.2 的不在这里重复列** —— 附在各榜榜尾（§4.2），②只列「本轮更新失败」与「够不上 §4.1」 |

### 待补

| # | 待定的事 |
|:---|:---|
| — | **暂时没有。** 这一版该定的都定了（文件夹名、脚本名、配置、资格线、排序、指数口径、留档、总结段）|
| 1 | **§3.2 交叉核对现在没有可比的东西了** —— `reports/per_blogger/` 八份与 `reports/博主对比.md` **已删**（2026-09-14 定：与其挂一批旧口径产物，不如不挂）。删之前已查实：原先对不上的三位**不是缺陷**，报告链是现算的（新口径）、产物是 09-13 的旧的 —— 山顶望星空的诗人与故乡的云ZYH 差在**参考价**（09-14 口径反转，135 条参考价变了），云帆观市差在**条数**（323 → 322，删顺延后它 2026-02-12 21:36 那条 `nfriday` 的终点落在春节休市，由计分变报错）。**处置：产物删除，等下一次全库重刷时重新生成。** 附带修了 `invariant.py` 的一处误报：报告文件不在时改判「报告还没出」（不算失败），原先落进「只有一边有那五个数」—— 报告一删八位全红，那是把「还没跑」说成「算错了」。重刷要付的代价是判断缓存整批重判 —— 规则指纹已随 02 的规则文本改动而失效 |
| 2 | ~~04 的代码还没跟上「榜不分指数」~~ —— **已结案**：`pool.py` 的 `index()` 与 `p["board"]` 已按同日那条改动记录全部改完（`boards.py` 五处改 `p["scored"]`，`compare.index` 键已从 `config.json` 删掉）；原附注的「`reports/博主对比.md` 随下一次全库重刷对齐」并入 #1 |


---

## 05 · 回测

### 改动记录

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-14 | §0 末尾那句「与 03§4、04§3.1、06§2 同一口径」改成「**与 06§0、06§2 同一口径**」—— 04 的榜改吃全部计分信号（见 04 同日那条），「只考察上证」这条线上只剩本文与推送；另外原来指的 06§2 是「两个板块」那节，上证那半句在 §0，故补上 §0。回测自己照旧只吃上证 —— **代码同批同步**：`backtest/flow.py` 定常量 `IDX = "上证指数"`，`_load()` 的筛选用它，不再读 `compare.index`（`backtest/conf.py` 的 15 键里本来就没有指数键） |
| 2026-09-14 | **通读校对**（见 01 同日那条的缘起）。本份五处：① §3 表里「**终点已过**｜验证终点已经收盘（含正好收盘那一刻）（03§3.6 ③）」改成「**本档时刻不早于验证终点的收盘时间**（含正好收盘那一刻）—— 03§3.6 ③ 的**实时版**」—— 03 那条比的是 `pub`（发帖晚于终点收盘 → 无效-过时），这里比的是本档时刻，同一个关系换了个左项；照抄 03 的措辞会让人以为要拿发帖时间判；② §3「终点算不出的（超出日历末日）不按后两条淘汰」改成「**算不出跨度的压根进不来** —— 这三条只判跨度落得进本板块的信号」—— 第 1 步已经卡死「跨度 ≥ 2」，跨度为 `None` 的（`long`、终点超日历）根本进不了这张表，原来那句在描述一个到不了的场面；③ §5.1「**那一档时刻该指数的现值**」改成「**本档标的（§5.2）的现值**」—— 标的由 `backtest.long_symbol`／`short_symbol` 定（现值中证1000），写「该指数」会被当成上证；④ §6 `参数.json` 的清单补全成「§9 两节列的那些键一个不少」并逐样点名 —— 原清单漏了 `update` 开关、`grid` 档表、`bucket_span` 两档边界三样，而 §6 ② 又说报告里那份「与 `参数.json` 同一份内容」；⑤ 删四处「为什么」（§0「拿不同时间点的信号回测…」、§0「调阈值时反复跑，抓帖是白等的」、§1「回测是事后跑的，增量只会引入前视」、§3「更早的档必然空仓，白拉一段平坦净值」、§5.3「胜率与换手率的分母才完整」）。**已核实无缺陷、不改**：§7「每天取收盘档（15:00）的净值」—— 15:00 之后价格冻结在收盘价，净值在 15:00 与 22:00 必然相等，取哪个都一样，与 `backtest/metrics.py` 一致 |
| 2026-09-14 | **淘汰由两条改三条**（06 那条的 05 侧，来龙去脉见 06 节）—— §2 那张「与推送同源的四样」里「06§5.7 **两条**淘汰」改「**三条**」、验证终点那行的用途由「淘汰『终点已过』」改成「淘汰『终点没开市』『终点已过』」；§3 步 1 与 §3 那张表同步补上「**终点没开市**｜验证终点落不到交易日 —— 这条表述不成立（03§3.6 ②）」，次序与 03§3.6 一致（没开市在已过之前）；末句「不按第二条淘汰」改「不按**后两条**淘汰」。**05§2 写着「规则同一套，机制两条路，结果必须一致」** —— 06 那边补齐了，这边不跟着补就是 05 自己和自己打架 |
| 2026-09-14 | §8「一条帖跑几遍 `runs = 1`（02§10.2）」改「一条帖跑几遍走 `parse.runs`（02§12）」—— 该键由 03 移归 02，口子仍只有一个（改配置），见 02 节 |
| 2026-09-14 | §2 表「验证终点」那一行：03§3.2（由 `spec` 定，**非交易日顺延**）→（由 `spec` 定，**不顺延**）。回测从 03§3 只借「验证终点」一样（§0），用来淘汰「终点已过」（§3）—— 终点不挪，淘汰的时点跟着变，故本文跟着改口径。来龙去脉见 02 节「删除顺延机制」 |
| 2026-09-13 | §2 的「**时间档**」改称「**推送档**」（06§5.1 那张时刻表）—— 与 06 统一叫法，也好和 02§4 的「周期档位」分开 |
| 2026-09-13 | §8「**抽取共识次数** `runs = 1`」改说「**一条帖跑几遍** `runs = 1`（02§10.2）」—— 「抽取」「共识」两个词只在这一处出现，正主在 02§10.2 |
| 2026-09-13 | **立稿**。取代 `.claude/skills/analyze-blogger/Swing_Timing.md`（该件自称「早期版本，正在被新规格取代」，05 立稿后即作废） |
| 2026-09-13 | 定案：**节奏不用参考件的「每交易日 14:30 一次」**，改用推送那张时刻表（06§5.1）—— 贴近真实的推送更新频率，不在尾盘一次定调 |
| 2026-09-13 | 定案：**判定与成交只发生在交易日的 20 档**。参考件里没有非交易日的档；非交易日不开市、没有价可成交 |
| 2026-09-13 | 定案：**成交价 = 那一档时刻的现值**，调 02§2.1 的取价函数（与 03§3.1 参考价同源）。参考件的「决策日 15:00 收盘成交」不成立 —— 档遍布全天 |
| 2026-09-13 | 定案：**回测不看得分**，只用 `d`；从 03§3 只借**验证终点**一样（判观点过期）。参考件没写这一条 |
| 2026-09-13 | 定案：**只考察上证、只取波段**（跨度 ≥ 2），超短期不进回测。与 03§4／04§3.1／06§2 同一口径 |
| 2026-09-13 | 定案：**数据来源是 `data/signals/<博主名>.json`** —— 参考件写的 `data/direction_signals/` 是旧栈路径，作废 |
| 2026-09-13 | 定案：**博主池另配一份**（`backtest.pool`），不复用 `push.pools.swing` —— 回测要换池子看结论稳不稳，推送的名单不能跟着动 |
| 2026-09-13 | 定案：**回看窗口复用 `push.window.swing`** —— 一个窗口喂两处，不会分叉 |
| 2026-09-13 | 定案：**观点取舍完全照搬 06§5.6／§5.7** —— 取最新三级判据 + 滑出窗口、终点已过两条淘汰。观点有效期 = 到它自己的验证终点为止 |
| 2026-09-13 | 定案：**`e = 0` 不动作**（ρ 无定义），持仓照留 —— 与参考件一致 |
| 2026-09-13 | 定案：**持仓只有一个** `pos ∈ {+1, 0, −1}` —— **多空不可能同时存在**，所以整体净值是两段的纯分解（`整体 = 多头段 × 空头段`），不存在两条腿怎么加权的问题 |
| 2026-09-13 | 定案：**多空标的各自可配**（`backtest.long_symbol` / `short_symbol`），现值都为中证1000 —— 参考件只写死一个交易标的 |
| 2026-09-13 | 定案：**同一档先平后开**，允许 +1 ↔ −1 在同一个档完成，中间不隔空仓。照参考件 |
| 2026-09-13 | 定案：**四个阈值存成分数**（`[分子, 分母]`），按整数比 —— 「严格不等」在恰好等于阈值时才是精确的 |
| 2026-09-13 | 定案：**阈值与法定人数全部参数化**；参考件写死的 `Q_open = Q_exit = 10` 是 30 人池子时代的数，池子一变就得重调，所以进配置 |
| 2026-09-13 | 定案：**基准两条** —— 多头标的买持、空头标的的反向买持（做空对照）；不是参考件那种「买持基准 = 交易标的同期首/末收盘」 |
| 2026-09-13 | 定案：**不做 VaR**。07 术语表里挂着的那三项（VaR／卡玛比／换手率）就此结案：**卡玛比与换手率做，VaR 不做** |
| 2026-09-13 | 定案：**夏普按日聚合再年化**（每天取 15:00 收盘档的净值 → 日收益 → ×√242，无风险利率取 0）—— 按档年化会大 √20 倍，没法跟外部比 |
| 2026-09-13 | 定案：**胜率按「一笔」算**（一段连续同向持仓 = 一笔）；**换手率 = 改仓次数 ÷ 回测年数** |
| 2026-09-13 | 定案：**指标三条曲线各算一套**（整体／多头段／空头段）；参考件的 `long` / `both` 两模式**取消** —— 双腿各自的表现直接并列展示，不需要「只做多」这个模式 |
| 2026-09-13 | 定案：**净值图横轴按档**（一天 20 点），五条线（整体／多／空／两条基准）—— 与判定频率一致，盘中换手的痕迹看得见 |
| 2026-09-13 | 定案：**产物落 `backtest/runs/<日期>_<时刻>/`**，一个夹子一次跑（`回测.md` · `净值.png` · `参数.json`）。**带日期留档，不覆盖** —— 不同的回测互相独立 |
| 2026-09-13 | 定案：**`参数.json` 记全本次参数** —— 「换池」不是功能，池子就是参数；每份档记清自己的参数，两份档自然可比 |
| 2026-09-13 | 定案：**数据完整性不管** —— 抓全与否归 01，回测只读 `data/signals/`。报告头部注明数据截止与「实际有数据的是谁」即可（旧栈的「干净日」概念不进新栈） |
| 2026-09-13 | 定案：**跑之前先全库更新一遍** —— 拿不同时间点的信号回测，等于拿一份残缺的分布做决策 |
| 2026-09-13 | 定案：**全库更新由回测自己调**（04§2 那一步），不是手动先跑一条命令。**§8 因此只剩一条命令** `python -m backtest`；§0 与 §1 一图流跟着改（一图流把「全库更新」提为 ①，档内的六步顺移为 ②～⑦）。那一步的说明归 04§2，本文不写第二套 |
| 2026-09-13 | §3 定案：**明写「先筛后取最新」，并把 06 一并改过来** —— 06 原写法是「先取最新再淘汰、顶掉旧的」，在「最新那条终点已过、次新那条还活着」时会把博主整档丢掉（实测库里确有博主会撞上）。**规则同一套、机制两条路（推送实时增量、回测事后重算）、结果必须一致**，写进 §2 |
| 2026-09-13 | §3 定案：`e` 的措辞改为**「分布里的条数 = 看多 + 看空」**，与 06§5.8 对齐（原写「有观点的博主数」）。全库 1454 条信号的 `d` 只有 ±1，故不影响结果 |
| 2026-09-13 | §5.1 定案：**完全不计成本** —— 手续费与滑点整个不进来，净值、逐笔收益、各项指标全是毛数。逐笔的「收益」= 标的在持仓区间**按方向算的变化率**。连带：原 §5.4「成本」整节删掉；§9.1 去掉 `backtest.fee`／`backtest.slippage` 两个键（12 → 10 键）；`参数.json` 不再记成本；待补 #2 就此结案 |
| 2026-09-13 | §5.3 定案：`begin` 空 → **信号能覆盖到的最早那天**（原写「行情能覆盖到的最早交易日」，与 §9.1 的现值 `2026-01-01` 打架，且会把 2024-06 起那一年半空仓段拉进净值、稀释年化与夏普）；`end` 空 → 行情末日。**年化用的「交易日数」= `[begin, end]` 闭区间内的交易日个数** |
| 2026-09-13 | §5.3 定案：**区间末尾还持仓 → 按末档现值强行平掉，记成完整一笔**（不留悬口，胜率与换手率的分母才完整） |
| 2026-09-13 | §4／§6 定案：**同档换向在账上算两次改仓** —— 逐笔表上两行（旧笔收尾、新笔开张），换手率计 2，中间不隔空仓 |
| 2026-09-13 | §7 定案：**两段各看自己那段账**（多头段只数持多的笔与改仓）；**整体那一列两向合起来算** —— 胜率按两向所有笔、换手率按两向改仓次数合计 |
| 2026-09-13 | §6 定案：**净值图横轴不画非交易日那 5 档** —— 否则周末与长假会拉出一排水平台阶 |
| 2026-09-13 | §8 定案：**加一个 `backtest.update` 开关** —— 关掉只省抓帖解析那一段，**行情照刷**（调阈值反复跑时抓帖是白等的）。§0／§1 一图流／§9.1 跟着改；原那句「回测不给自己开开关」收窄为「除这一个开关，不开别的口子」。`参数.json` 一并记上这个值 —— 关掉跑出来的档与全量跑出来的档要能各自说清 |
| 2026-09-13 | §8 定案：**回测不给自己开开关** —— 调 04 那一步时 `begin_date` 走 `scrape.begin_date`、`runs = 1`、先刷行情，三个值都写死；要改就改配置 |
| 2026-09-13 | §0 定案：**代码包与 `blogger/` 同级**（`python -m backtest`），不塞进 `blogger/` 里 —— 回测是独立的一条线。**产物跟着落包内的 `runs/`**（原写 `backtest/<日期>_<时刻>/`，与代码同一层，改掉），§6 与 §8 一并跟上。推送同此例（`python -m push`） |

### 待补

| # | 待定的事 |
|:---|:---|
| 1 | **阈值的生产值** —— 现在配置里那组（2/3、1/3、1/2、`Q = 3`）是**测试值**，按 8 人池子定的。池子补起来后要重定，尤其是两个法定人数 |
| 2 | **换池实跑** —— `backtest.pool` 已经另配，但库里只 8 位有数据，眼下无池可换。等池子补起来再验「换一批人，结论还成不成立」 |
| 3 | **净值图会引入 matplotlib** —— 环境里已有 3.9.2，但 `blogger/` 此前一处绘图都没有、仓库也没有依赖清单。回测是第一个用它的地方，要不要顺手落一份依赖清单 |


---

## 06 · 推送

### 改动记录

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-14 | **三处收口**（用户裁定）。① **§1③ 与代码不符** —— 原写「每位取窗口内最新的『上证 + 本档』信号」，但初始化与每一档一样是**逐条并进**（`_merge`），「取最新」发生在 ⑧ 计数那一步；改成「窗口内本板块的信号逐条落进去（每位不止一条）」。② **§6.1 样例卡换掉一行，并点明它是示意** —— `子房论市` 不在库里（`data/posts/` 没有他），那条引文「沪指3900点是多空分水岭，守住还能反弹」全库搜不到，而且它正是 02§6 明令不产的条件式；换成库里真有的 `山顶望星空的诗人`（09-06 19:08、看多、终点同为 09-11），排在 `我觉醒了` 之后以合「名字顺序＝池子里的顺序」。核对时另查出两处不符、**经用户裁定保持现状**：`知行合一` 与 `我觉醒了` 那两行取的是**被后帖覆盖的旧条目**（两人此后各又发过一条，真取中的是后者），且 09-09 14:00 那一档实跑是 8 位 4多/4空，卡上只 4 位 2多/2空。处置是**在 §6.1 前加一句注明「这一张是示意卡，不是某一档的实跑产物」**，卡面一字不动。③ **重试口径统一**（见 01 同日那条）：§5.10 与 §7 表「重试 3 次」改成「连同首次一共试 3 次」，§9 表 `push.retries` 的「重试几次」同改。代码侧顺带：`push/flow.py:_pick` 的 docstring 原写「取窗口内 `pub` 最大」，实际调的是 `consensus.latest` 的三级判据，改成「取最新的那条（§5.6 第 3 步的三级判据）」 |
| 2026-09-14 | **通读校对 ＋ 三处代码对齐**（六份一起做，规则一条不增不减）。本份十处：① §3 补「日历覆盖不到那么早 → 窗口算不出来 → 本档不推」；② §4 连带后果随之改写；③ §5.1 补「**两张表都不含现刻时，不碰任何东西就退**」与「**`--force` 与 `--init` 都不走这道检查**」；④ §5.4 补「从没抓全过的博主……全量重抓」＋抓完校验（01§9）；⑤ §5.6 第 3 条判据改成「仍并列 → **先出现的赢** —— 信号文件与状态文件按同一个顺序排（`pub`／`idx`／`spec`），两条链在这条判据上同序」；⑥ §5.7 补「**另有一道前视** —— 本档只看 `pub` ≤ 本档时刻的帖」（与 05§3 同一条）；⑦ §6.1 与 §6.3 样例卡删掉多余空行；⑧ §7 表补三行；⑨ §8 那句「不写任何东西」改成实指 —— 「状态与留档都不落；抓帖与行情照走、照写 `data/posts/` 与 `data/market/`」；⑩ §9 引子改成「**另有五样写死在 `push/conf.py` 里**」。代码侧三处：`flow._drop` 补上 `nontrading` 一道（原先只做窗口与过期两道，**推送与回测会分叉**，违反 05§2）、`run()` 在补日历之前加一道廉价预检（两张表都不含现刻就先退，§5.1 那句话才成立）、`_scrape` 抓完就地校验；`state.save` 的排序键由 `(pub, spec, d)` 改成 `(pub, idx, spec)` —— 与信号文件同序，§5.6 第 3 条判据才与回测那条重算的路一致 |
| 2026-09-14 | §4 里那条 `--init` 的括注补上「或『一条帖跑几遍』」—— 缘起、实测与全部五处改动见 03 节同日那条 |
| 2026-09-14 | §5.3 那句「01§10 那一套（日线、30 分钟线、补到运行当日并核对）」改成「（**30 分钟线**、补到**该到哪天**并核对）」—— 同日砍掉日线，见 01 那条 |
| 2026-09-14 | **跟着 04 的榜放开收两处引用**（04 那侧见同日那条）。① §0「与 03§4 的榜、04§3.1 同一口径」改成「**与 05§0 同一口径**」—— 04 的榜改吃全部计分信号，不再和推送同一条线了；现在与推送同口径的只剩回测（05 也是只吃上证）。② §9 删掉 `compare.index` 那一行与底下「只看哪个指数与 04§3.1 是同一个键」那半句 —— 该键已从 `config.json` 删掉（04 不再有指数这个维度），**推送把「上证」写死在正文里**（§0），不开配置键。**代码同批同步**：`push/conf.py` 定常量 `IDX = "上证指数"`，`load()` 的 `"index"` 改用它，模块 docstring 那句「照 04§3.1 那个键（`compare.index`）」改「**只看上证指数**是写死的（06§0），不开配置键」；**顺带修一处静默错误** —— 同文件的 `quote_limit` 原先读 `report.quote_limit`（键不存在，靠默认值 60 恰好等于真值），改成 `parse.quote_limit`（02§12），`push/card.py:_clip()` 的 docstring 同步。另同批修掉 `push/card.py:_直到()` 那个到不了的兜底（`{spec}（终点算不出）`）—— 算不出终点的压根进不了状态（§2、§5.7），改成直接印 `终点 MM-DD 收盘`；`_直到()` 与 `consensus.expired()` 两处「日历含未来一年」改「含未来」 |
| 2026-09-14 | **通读校对**（见 01 同日那条的缘起）。本份十一处：① §5.7／§6.2 卡片示例 **故乡的云ZYH 终点 09-18 → 09-11** —— 原话含「下周」落 `nweek`，发帖 09-05，终点就是那周最后交易日 09-11（库里的信号文件也是 `nweek`／09-11），09-18 是下下周；② 同卡 **我觉醒了 的发帖时刻 09-08 17:47 → 09-05 17:47** —— 原话含「下周」，09-08（周二）的「下周」落到 09-18，与卡上写的 09-11 自相矛盾；库里那条真信号就是 09-05 17:47 `nweek` → 09-11；**同一张卡里四位博主的次序也换了**（故乡的云ZYH → 子房论市 → 知行合一 → 我觉醒了）—— §6.2 写着「顺序就是池子里的顺序」，而 `push.pools.swing` 里子房论市（第 5 位）排在知行合一（第 8 位）前面，原示例是随手写的；③ §5.7「终点已过」判据与 §8 同步改成 03§3.6 ③ 的**实时版**（同 05 同日那条）；④ §5.7「终点算不出的（超出日历覆盖）不按后两条淘汰…日历含未来一年，所以这只在『终点超过日历末日』时发生」改成「**算不出跨度的压根进不来**（§2）」—— `push/conf.py:span_ok` 对 `span is None` 一律 False，这类信号第 2 步就被挡在状态外，原来整段在描述到不了的场面，§6.2「只有终点超过日历末日时才退回写档位」同理删掉，「日历含未来一年」一并改「含未来」；⑤ §0 表两行（终点价／收益率、得分）的粗体位置与其余五行不齐；⑥ §0「与 03§4 的榜、04§3.1 同一口径（04§3.1）」删掉重复的括注；⑦ §5.5「单帖上限、排序方向」改成「单帖上限、批与批之间的间隔」—— 03§2.2 那张表里没有「排序方向」这个项，有的是批间隔，§9 末条也跟着补上第四样；⑧ §5.1 节标题「不在档上就直接退出」改成名词短语「在不在档上」（同节 5.2～5.10 全是名词短语）；⑨ §5.6 删掉内部自引「（§5.6）」（它就在 §5.6 里）；⑩ §5.6 末尾那两段 blockquote（「滑出窗口不必回头重扫」「后两条没有这个性质」）删掉 —— 装的是论证不是不变量，规则本身上一句「状态里留窗口内的好几条」已经说完；⑪ §5.8「分布里 `d = +1` 的条数」改成「**上卡的条目**里 `d = +1` 的条数」—— 「分布」在 §5.6 指状态里那几条、在这里指取完最新后的一人一条，同一个词两个意思⑫ **§1 一图流的第 ③ 步由「备料行情 ── 补到运行当日并核对（01§10）」改成「取现价 ── 供解析新帖的行情注记（§5.3）」**（取不到怎么办**暂不写死** —— 待补 #3 里连同取法一起定），§8 里那句「`--force` …其余一步不省：**行情照核**、增量照抓、窗口照算，该硬失败的还是硬失败（§5.3）」跟着改成「**现价照取**」—— §5.3 明写推送取现价、「既不读 01§10 那一套、也不等它」，一图流与 §8 却还在说备料行情，三处互相打架；⑬ **§5.3 末尾那句收成「现价与交易日历都由推送自己取」** —— 2026-09-14 定案：**06 不走 01 的备料链**（01 不为 06 单独留口子），实时现价源与推送自己那套交易日判断都还没设计，记进本份待补 #3。**代码现状与⑫⑬相反**：`push/flow.py` 第 ③ 步走的是 `blogger.market.fetch.refresh`（同一份 30 分钟线），要按本文改 |
| 2026-09-14 | **§0 立一条总口径：一条帖怎么读、读完怎么算，06 与 03 是同一套** —— 同一条规则、同一份判断缓存、同一个 `pub`，**两边结果必须一致**。加一张七行表逐项点名：解析、验证终点、交易日跨度、复盘／过期的筛选四样**一致**（各指 02／03 的正主）；**参考价**是**唯一的分叉** —— 06 取现价（推送时刻实时报价）、03 取发帖时刻那根 30 分钟线；终点价 06 **不取**（终点还没到，没有价可读）、收益率与得分 06 **不算**（推送不打分）。§5.3 与 §5.5 各加一句回指这处（§5.3 点明「这是唯一分叉」；§5.5 点明「复盘／过期」02 已在提取端拦过一道、**§5.7 是最后一道** —— 照 03§3.7 的口径，不拿「上游已经拦过」当「这里不必再拦」。初稿此处写反了，写成「推送到手本来就没有这类，不再筛一道」，是把 03 明确否掉的立场搬了过来）。**顺带补一处两边对不上的地方**：§5.7 淘汰原先只丢两类（滑出窗口／终点已过），漏了 03§3.6 ② 的「**终点落不到交易日 → 报错**」—— 那类信号的交易日跨度算得出，06 会当正常立场照上卡，而 03 判它不成立、不进统计。现补成三类，判据与次序都照 03§3.6 的状态机；§1 一图流 ⑥、§5.6 步 1 同步。**并且第 2 步并入的也要先过这三条判据** —— 淘汰在并入之前跑，新产出里就不成立／已经过期的若不当场丢掉，会活到下一档才被清，这一档就上了卡。§5.6 那两条「哪一条淘汰不必回头重扫窗口」的注记跟着改成按名字说（第三条不再叫「第二条」）。**代码同批同步**：`blogger/common/consensus.py`（05 与 06 共用这一份）标题与文件头「两条淘汰」改「三条」，新增 `nontrading(ep)` —— 判据与 `verify._state()` ② 逐字相同（`after_calendar` 为假 且 不是交易日），`candidate()` 按 §5.7 的次序接在 `in_window` 之后、`expired` 之前，`__all__` 补上。**实测这第三条目前是空转的**：`market.span()` 要求 `ep` 在交易日历内，所以**终点落非交易日的信号跨度一律是 `None`**，早就被「跨度落在本板块」那道门槛挡住了 —— 05 池子里这样的信号共 3 条（云帆观市 2026-02-12 `nfriday`／山顶望星空的诗人 2026-08-27 `d:2026-09-20`／江河之水终有入海之日 2026-03-01 `monday`），**没有一条够得上回测口径，净值一条都不动**。留着它是照 03§3.6 ② 的例：上游拦过一道，这里仍留最后一道。§2 那句「算不出跨度的信号两张卡都上不了」跟着点名第三个来源（终点落不到交易日），并说明 §5.7 另有同判据的兜底 |
| 2026-09-14 | §5.3 由「**备料行情**」改成「**取现价**」—— 推送的行情注记一律取现价（02§2.1），实时报价、与 30 分钟线无关；**01§10 那一套历史行情备料（日线、30 分钟线、核对）只服务报告与回测**，推送既不读它、也不等它。原写法让推送按「本档时刻该覆盖到哪根 30 分钟线」去核对行情，是照搬报告链的口径（01§10.4 那两行同批删掉）。连带：§6.2、§8 三处引文长度改引 `parse.quote_limit` |
| 2026-09-13 | **新增 `--force`** —— 不在时刻表上时，**跳过早退检查**（§5.1），把此刻当成一档推出去；§8 补上这条命令与「其余一步不省、该硬失败的还是硬失败」的边界。此前 06 把「不在档上就退出」写成了没有例外的规矩，而术语表里一直挂着一条「手动补推」（不在节奏上人为跑一次推送）—— 两者对不上，现按补推落地 |
| 2026-09-13 | §2 补一句：**上不了卡的信号照进报告** —— 推送是报告（03）在时间轴上的一个切片，不是另一套统计 |
| 2026-09-13 | §3「先**锚定**到它之前最近的那个交易日」改说「先**退到**……」—— 「锚定」在别处指的是「把相对说法换成具体日期」，同一个词两个意思 |
| 2026-09-13 | §7「不**回落**另一个群」改说「不**转投**另一个群」—— 「回落」是博主术语（冲高回落），不该拿去说程序行为 |
| 2026-09-13 | §5.3 **重写**：核对基准塌缩成一句话 —— 「**把本档时刻当成一个发帖时刻，看注记取不取得到**」（02§2.1），原先「盘中／盘后／午休三档分别写死」作废。**这一档要的那根 K 线 = 「结束时刻不晚于本档时刻」的最后一根**：`10:30` 档要 `10:30` 那根，`12:00`／`12:30`／`13:00` 档要 `11:30` 那根，`09:30` 档当天一根都还没有、退到上一交易日末根，`15:00` 及之后的档要 `15:00` 那根。**顺带消掉了 `09:30`／`13:00` 两档每天必然硬失败的毛病** —— 要的是**已经收出来的**那根，不是「本档时刻所在那根」，后者在那两档上根本还不存在。另三条：§4 补「判断缓存整批作废时要重跑 `--init`」；§6.3 最小卡末句按窗口天数现算；§9 补 `compare.index` 键（与 04§3.1 是同一个键，不另配一份） |
| 2026-09-13 | §3 定案：**超短窗口取 1 个交易日够了**（待补 #1 结案）。§5.3 定案：**核对基准按「当天已收到的最后一根 30 分钟 K 线」算，并把盘中／盘后与午休／盘前与非交易日三档分别写死** —— 30 分钟线按结束时间标注（每天 8 根，无 `09:30`），「本档所在那根」在整点档上有歧义（14:30 档要的那根到 15:00 才收得出来） |
| 2026-09-13 | §5.6 定案：**改成「先筛后取最新」三步（淘汰 → 并入 → 取值）**，原写法是「先取最新再淘汰、顶掉旧的」。**原因**：原写法在「最新那条终点已过、但次新那条还活着」时会把这位博主**整档丢掉**，明明他还有活着的观点 —— 实测库里确有博主会撞上（新的多是 `t2`／`week`，旧的 `month`／`t5`／`nweek`）。连带：**状态从「每位一条」改成「窗口内那几条」**（§4、§5.6、§5.7、§1 一图流的 ⑥⑦⑧ 三步）。**05 回测走同一套规则**（05§3），两边结果必须一致 |
| 2026-09-13 | **立稿**。取自旧栈 `02-推送全流程.md`，按新栈口径重写 |
| 2026-09-13 | 定案：**分档改用交易日跨度**（03§3.4），与 04 同一口径 —— 旧栈按卡片上的周期词分，两套判据会分叉（03 待补 #2 就此结案） |
| 2026-09-13 | 定案：**只考察上证指数** —— 与 03§4 的榜、04§3.1 同一口径 |
| 2026-09-13 | 定案：**初始化与增量分开** —— `--init` 建窗口（回看窗口内的帖只读一遍、判一遍），此后每档只处理新帖，滑出靠 `pub` 判 |
| 2026-09-13 | 定案：**`--init` 只从回看窗口起点抓**，不从 `begin_date` 全量抓；连带后果写进 §4 |
| 2026-09-13 | §0 定案：**代码包与 `blogger/` 同级**（`python -m push <板块>`），不塞进 `blogger/` 里 —— 推送是独立的一条线。05 同此例（`python -m backtest`） |
| 2026-09-13 | 定案：**命令是一条、板块当参数** —— `python -m push <板块>`，一次只推一个板块 |
| 2026-09-13 | 定案：**博主池写进 `config.json` 的 `push.pools`**，名单顺序就是显示顺序 |
| 2026-09-13 | 定案：**增量起点复用 01§5 的续抓起点** —— 与报告链同一个水位、同一处算法 |
| 2026-09-13 | 定案：**状态每板块一份** `data/state/push_<板块>.json`，与「两条独立的线」一致 |
| 2026-09-13 | 定案：**节奏沿用旧栈但参数化** —— 交易日 20 档、非交易日 5 档，两张表进 `push.grid` |
| 2026-09-13 | 定案：**非交易日两个板块都推**（旧栈非交易日只推波段） |
| 2026-09-13 | 定案：**卡片去掉行情行**，但行情仍要补 —— 用途写明两条：**行情注记**（解析新帖的前提，02§2.2）与**交易日历**（§5.3） |
| 2026-09-13 | 定案：**行情核对基准分链** —— 推送链核到**本档时刻**（盘中要当天分钟线覆盖到本档那根 K 线，盘前/休市要到上一交易日收盘），达不到硬失败；不跟报告链共用「收盘后才要求今天」那条（§5.3） |
| 2026-09-13 | 定案：**交易日历改用官方日历**（含未来一年，01§10.3）。连带：§6.2 的「算不出终点退回写档位」降为兜底（只剩终点超日历末日），§5.7 的「终点已过」正常生效 |
| 2026-09-13 | 定案：**「最新」三级判据** —— `pub` 大 → 验证终点更晚 → 信号文件里先出现（§5.6） |
| 2026-09-13 | 定案：**调度器是 Windows 任务计划**，注册脚本归部署层（§8） |
| 2026-09-13 | 定案：**状态文件不记「上次推送时刻」** —— 窗口起点每档现算，停机恢复不需要对账（§4） |
| 2026-09-13 | 定案：**卡片去掉「摘要」行**，每位博主只占两行 —— 新栈的结构化数据只有 6 键、没有摘要字段；去掉后卡上每个字都能逐步核对 |
| 2026-09-13 | 定案：**计数不写分母** —— 卡上只写「N多/M空」 |
| 2026-09-13 | 定案：**淘汰两道** —— 滑出窗口（`pub` 早于窗口起点）与终点已过（含正好收盘） |
| 2026-09-13 | 定案：**周期优先写验证终点**，算不出终点才退回写档位 |
| 2026-09-13 | 定案：**发卡失败不补发**；**留档** `data/briefings/<日期>_<时刻>_<板块>.json` |
| 2026-09-13 | 定案：**旧栈 9 条已知缺陷不搬** —— 新栈是重写，不是移植；真撞上了再列 |
| 2026-09-13 | 定案：**推送的池子与报告链的名单各自独立、互不干涉**，不约定谁先跑；连带后果写进 §4（待补 #2 结案） |

### 待补

| # | 待定的事 |
|:---|:---|
| 1 | ~~超短窗口取 1 个交易日是否够~~ —— **已结案**：**够**。跨度 ≤1 的信号里，「跨度 1 的那条在次日盘中是否仍有效」实测 151/151 全部有效（周五盘后说的「下周一」到周一盘中仍在窗口内、终点未到）。结论写进 §3 |
| 2 | ~~池子里的博主还进不进报告链~~ —— **已结案**：两个名单**各自独立、互不干涉**，不约定谁先跑。连带后果与「报告链要用这批人得删净重引」写进 §4 |
| 3 | **推送的实时现价源、以及推送自己那套交易日判断，都还没设计** —— 2026-09-14 定案：**06 不走 01 的备料链**。获取的东西与 01§10 会很相似（都是行情、都认官方日历），但**01 不为 06 单独考虑**，06 也不读它的产物。06 要自己解决两件事：① 解析新帖时的行情注记取**现价**（§5.3），从哪条实时报价取、取不到怎么办（本档不推？兜底？）、`push.retries` 那套重试适不适用 —— 全未定；② 本档是不是交易日（§5.1、§3 的窗口都要它），日历从哪取、什么时候刷、刷不上怎么办 —— 也未定。**代码现状与这条相反**：`push/flow.py` 的第 ③ 步 `_market()` 走的是 `blogger.market.fetch.refresh`（同一份 30 分钟线），`_covered()` 的注释还写着「注记取的是发帖时刻的现值，那是从 30 分钟线来的」—— 要按本文改掉 |

---

## 通用 · 格式规范

六份正文共用的写法。**只讲格式，不改逻辑** —— 逻辑的改动进各节自己的「改动记录」。

| 项 | 规矩 |
|:---|:---|
| **粗体** | 只留三种：行首标签、硬约束（绝不／必须／一律／不许／永不）、整格即标签的表格单元格。句中强调一律不加粗 |
| **表格** | 一列里要么整列加粗（它是标签列），要么整列不加粗。同一列里有的粗有的不粗 = 没写完 |
| **节标题** | 名词短语，不带副题。副题的内容搬到该节首句 |
| **破折号** | 一律 ` —— `（前后各一个空格） |
| **用词** | 「本档」不写「这一档」；阈值叫「阈值」，榜的条数／均分线叫「资格线」 |
| **一图流** | 每一步带序号 ①②③…，六份一致 |
| **跨文档** | 同一件事只在最正主的那一份详写，别处一行指过去（`本文不写第二套`／`与 X§Y 同一口径`） |
| **围栏块** | 原样保留，不受上面任何一条管 —— 里面是命令行、图示与真实产出 |
| **括号** | 只留四种：跨文档指针（`§x`／`见 03§4`）、默认值、取值范围、字段名。解释设计意图的一律删 |
| **`>` 块** | 只留不变量与硬约束（必须同值／不许／只列不排）。由来、论证、「否则会怎样」全删 |
| **时间词** | 指代运行日的一律写「运行当日」，不写「今天」 —— 「今天」随读者何时读到而变 |

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-14 | **按新增的三条再精进一遍**：`>` 块 44 行收到 29 行（只留不变量与硬约束），括号里解释设计意图的全删，指代运行日的一律改「运行当日」（01 三处、03 三处、06 四处） |
| 2026-09-13 | **按上表把六份正文过了一遍**（粗体 964 → 413、节标题全部压成名词短语、表格列内加粗拉齐、破折号与用词统一、01 一图流补序号、六张空表头补名）。逐份用事实比对脚本核过：围栏块、行内代码、数字、章节引用、配置键、表格行数**零变化** |

---

## 判例 · 多次判断不一致的信号

正文另立一份：[判例.md](判例.md)。**它不是规格** —— 是规则的立法理由与实例，
所以「改动记录」记它，不记在 01～06 任何一节里。

### 改动记录

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-15 | **首批裁定入档 + 剔除已可避免的**：`判例.md` 已写 40 条裁定（全在甲组、全出自云帆观市），逐条抽成 [判例经验.md](判例经验.md) —— 13 条是 02 已写着、模型没照做，16 条是 02 还没写的新规则（否定式方向词、骑墙不产、「最终」＝`long` 等），11 条只表态哪一遍对。按「只靠 02 现有条文就能定下哪一遍对」这条判据**剔除 18 条**（甲 14、乙 2、丙1 1、丙3 1），编号不重排、已裁定的 40 条一条没碰；正文 415 → 397。**没有动 02** |
| 2026-09-15 | **立档**：新增 `blogger/tests/jitter.py`（`run` 判一遍落 JSON、`cmp` 比两遍出争议分类、`draft` 出草稿），在**冻结的同一份输入**上把全库判两遍 —— 4166 条帖里 415 条（10.0%）判得不一样。逐条摊成 `docs/判例.md`，等人给裁定。边界说明：两遍都判到 4166 条、0 失败批；「库（参考）」那一列是 `data/signals/`，输入不干净（帖文 22:57 被重抓），只作参考、不算第三票。**`data/` 一个字节没写**（跑动前后 mtime 未变）。指针加在 `改动记录.md:3` 与 `02-llm解析.md` 末尾，`blogger/tests/README.md` 补一节 |

### 待补

| # | 事项 |
|:---|:---|
| 1 | **正文 397 条等人给裁定**（甲 155、乙 51、丙1 45、丙2 8、丙3 4、丁 134），裁定完再提炼成 02 的规则行 —— 负例进 §6、边界进 §3.1／§10.1、跨表口径进该节的引子。已抽出来的那批见 [判例经验.md](判例经验.md) 的待办 |
