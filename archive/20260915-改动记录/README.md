# 2026-09-15 改动记录归档

`docs/改动记录.md` 整份移入本目录 —— 2026-09-15 定：**不再维护改动记录**。

| 段 | 是什么 |
|:---|:---|
| 01～06 各自的「改动记录」 | 那一份什么时候改过、为什么改 |
| 01～06 各自的「待补」 | 还没定的事；定了的当场划掉、行留着不删 |
| 通用 · 格式规范 | 六份正文共用的写法（粗体只留三种、节标题名词短语、破折号、括号只留四种……） |
| 判例 | `docs/判例.md` 与 `docs/判例经验.md` 的立档记录 |

## 为什么挪走

**规格文档只管「现在该怎么写」。** 改过什么、还欠什么，不再单独立一份跟着走 —— 六份正文本就是应然，读者不看它的来历。

**格式规范随之进档。** 它已经全数落在六份正文里，也在 `docs/判例.md`、`docs/判例经验.md` 里照用；新写文档时取回来看。

## 归档时还开着的待补

**14 条未结**（「已结案」的那些见本目录 `改动记录.md`，一条没删）：

| 段 | 未结 | 是什么 |
|:---|:---|:---|
| 01 抓取 | 5 | 认不出身份的条目占比、请求风控（限速／重试上限／间隔）、旧数据全库重抓、旧 `publish_time`／`publish_date` 怎么迁、30 分钟线只剩新浪一个源没降级余地 |
| 03 单博主报告 | 3 | `long` 之外还有没有别的「不计分」、脚本名（`report`／`report_all`）待确认、「报错」要不要跟「无效-过时」一起删 |
| 05 回测 | 3 | 阈值的生产值（现在是按 8 人池定的测试值）、换池实跑、净值图引入 matplotlib 要不要落依赖清单 |
| 04 全博主对比 | 1 | §3.2 交叉核对眼下没有可比的东西 —— 八份报告与对比榜已删，等下一次全库重刷重新生成 |
| 06 推送 | 1 | 实时现价源与推送自己那套交易日判断都还没设计；`push/flow.py` 第 ③ 步现状与 06§5.3 相反 |
| 判例 | 1 | `docs/判例.md` 正文等裁定，裁定完提炼成 02 的规则行 |

02 那一节**已结案**（±10% 分不分档、「N日内」与 `tN`、映射表纳入判据、整周无交易日兜底、规则文本按 02 整份重新生成 —— 指纹 `603c3a6437ec77ef` → `9ff2cf78ce60f200`）。

## 取回

```bash
git mv archive/20260915-改动记录/改动记录.md docs/
```

**平时不用取回。** 要查某份规格为什么是现在这个样子、或者拿格式规范对照着写新文档时再看。
