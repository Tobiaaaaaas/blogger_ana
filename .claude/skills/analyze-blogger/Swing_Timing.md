# 波段择时回测（仅用于回测，不用于实时推送）

> ⚠️ **本文件是早期版本，正在被新规格取代。**
> 回测那一份新规格**尚未写出**（按 `docs/03-单博主报告.md` §0 的编号，它排 05），本文暂为回测的唯一依据。
> 术语看不懂时查 `docs/07-术语对照表.md` 的「旧称」列 —— 新规格已把自造术语全部换成大白话。
> 新旧并存的目的：新规格定稿前，本文件仍可用于对照。

## 1. 口径与参数

| 项 | 值 |
|---|---|
| 波段委员会 | 指定的 30 位博主（2026-09-06 更新，与 briefing PANEL_SWING 一致）：一只小小牛、股往金来oo、家有高中生2、故乡的云ZYH、子房论市、选对时机买对股、香满衣、知行合一、智由智哉、股评老陈、云帆观市、赵红力、趋势巡航、孙万林、大盘蜂向标、谭阿坤、白猫财眼、禅壹、爱生活的荷叶Rp、时间轨迹、四十二流光、我觉醒了、中国技术玩家、山顶望星空的诗人、衡山佛曰论股、诸葛不亮、江河之水终有入海之日、时空鹰眼、刘海娃娃、顺应周期 |
| 判定节奏 | 每**个交易日**一次 **14:30** 快照定调 |
| 波段观点 | 窗口 w 内按 `pub` 从**最新往最旧**扫描，取**第一条**同时满足下列全部条件的行，该行的 `d`（+1/−1）即该博主的波段观点：① 波段 = span ≥ 2，详细定义见SKILL.md §8；② `idx == "上证指数"`；③ **目标未过期**——`target` 为 `null`，或 `target ≥ 决策日`。**过期候选不是终止信号，跳过它继续往旧扫**；整窗无合格行 → 该博主在期间无波段观点（不进e）。每个博主每个交易日最多只有一个波段观点。 |
| 分母 e | 窗口 w 内波段委员会所有波段观点的数量，数学上必须≤波段委员会的数量 |
| 窗口 w | **5 交易日**（决策日前第 5 交易日 00:00 → 决策 14:30 前，墙钟含周末帖） |
| 看多比例 ρ | = **多方观点 / e**（看空比例 = 1−ρ = 空方观点/e，互补）——开/平条件以它为唯一自变量（持有 = 未触发平仓门的默认状态；e=0 时 ρ 无定义、当日不动作） |
| 信号标的 | 用于发表观点的标的为"上证指数" |
| 交易标的 | 用于回测的交易标的为"中证1000"，为了增加弹性 |
| 成交/模式 | 决策日 **15:00 收盘**成交 · `long` 仅做多 / `both` 双向（指数期货式，未计融券费/保证金）· 0 成本、全仓 0/1 |
| 净值/基准 | 收盘→收盘；样本末持仓按末日收盘强制平 · 买持基准 = 交易标的（中证1000）同期首/末交易日收盘 |

> **数据来源**：所有博主的所有波段信号、观点以及对应的具体时间已经存在于 `data/direction_signals/<博主名>.json`，请直接提取。

**阈值参数**

| 参数 | 值 | 触发条件 |
|---|---|---|
| 多头开仓 `TO_LONG` | **2/3** | （空仓或刚刚平仓完）且ρ **>** TO_LONG 且 e>Q_open → 开多 |
| 空头开仓 `TO_SHORT` | **1 − TO_LONG = 1/3** | （空仓或刚刚平仓完）且ρ **<** TO_SHORT 且 e>Q_open → 开空 |
| 多头平仓 `TX_LONG` | **1/2** | 持多且ρ **<** TX_LONG 且 e>Q_exit → 平多 |
| 空头平仓 `TX_SHORT` | **1 − TX_LONG = 1/2** | 持空且ρ **>** TX_SHORT 且 e>Q_exit → 平空 |
| 开仓法定人数 `Q_open` | **10** | e **>** Q_open为开仓的必要不充分条件 |
| 平仓法定人数 `Q_exit` | **10** | e **>** Q_exit为平仓的必要不充分条件 |


## 2. 状态机

```
当日判定，分以下三步：

第一步：输入：现持仓 pos∈{0,±1}，当日 e、多方观点 bull → ρ = bull/e。

第二步：如有持仓（pos≠0）：
平仓门：
  if 持多 且 ρ < TX_LONG 且 e > Q_exit → 平多（目标 0）
  if 持空 且 ρ > TX_SHORT 且 e > Q_exit → 平空（目标 0）
  其余情况不平仓

第三步：
如空仓（pos=0）或刚刚平仓完：
开仓门：
  if ρ > TO_LONG 且 e > Q_open → 开多（目标 1）
  if ρ < TO_SHORT 且 e > Q_open → 开空（目标 -1）
  其余情况不开仓
```

> 所有比较均为**严格不等**：ρ 恰等于阈值（如恰 2/3、恰 1/2）不触发开/平，维持原持仓。

> **同收盘换向**：第三步"刚刚平仓完"允许同日换向——先走平仓门（需 e>Q_exit），平出后再走开仓门（需 e>Q_open），两门都过且方向相反 → 同一 15:00 收盘完成 +1↔−1 换向（无隔日空仓）；只过平门 → 平至持币；`long` 模式开仓门不判开空，持多平出后只到 0 持币。

- `long` 模式 pos∈{0,1}：开仓门只判开多（ρ<TO_SHORT 不触发开空、保持持币）；平仓门与 both 相同（持多且 ρ<TX_LONG 且 e>Q_exit → 平多）。

---

> **回测输出要求**：回测完成后，请生成 `long`（仅做多）与 `both`（双向）两种模式下的完整策略评估报告，含：**净值图**、累计/年化收益、VaR、最大回撤、夏普、卡玛比、胜率、换手率等常见的绝对收益产品的评估指标。
