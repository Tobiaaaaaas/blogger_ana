# 01 · 抓取

**管什么**：把博主发的帖子从今日头条搬进 `data/posts/`，并让全库数据保持齐。
**不管什么**：怎么从帖里读出观点（见 02）／拿这批帖做什么（见 03–06）。

---

## 01§0 边界

**输入**：博主的**任意一条帖子的链接** ＋ **一个起始时间**（参数名 `begin_date`）。链接用来定位博主本人；起始时间决定往回翻到哪。

**抓取永远是区间式的** —— 抓 `[起始时间, 至今]` 这一段。**抓多少就解析多少**，抓取与解析是同一个区间。

| 谁调 | 抓谁 | 起始时间从哪来 |
|:---|:---|:---|
| **单博主报告**（03）· 新博主 | 那一位 | 固定日期（如 `2026-01-01`）—— **全量** |
| **单博主报告**（03）· 旧博主 | 那一位 | 该博主 `scrape_time` 所在那一日的零点 —— **增量** |
| **推送**（06） | 名单内每位博主 | 同上 —— **增量** |
| **补齐**（手动） | 全库每一位 | 同上 —— **增量** |

**起始时间一律由调用环节算好传进来。** 抓取自己**不看 `scrape_time`、不判断该抓多少** —— 决定「这次是全量还是增量、从哪儿增量」是调用环节的事。

**起始时间是硬边界，但它在这里判一次就够。** 早于它的帖一律不入库（§4）；这一层做干净了，下游（03／06）拿到的主文件**天然都在区间内**，不必再筛一道。

**抓取从不自己发生**：由报告流程调、推送流程内部调、补齐手动跑。没有常驻的定时抓取。

---

## 01§1 一图流

```
博主链接 + 起始时间
   │
   ▼
翻列表（每页全收），逐条过四道关 ── 非本人 / 时间不实 / 列表页噪声 / 置顶帖 ──► 丢掉
   │
   ▼
收完统一过一遍：早于起始时间的帖丢掉
   │
   ▼
逐帖进详情页取全文（视频帖不入库）
   │
   ▼
按 post_id 去重，并入主文件 ── 旧帖原样保留，永不覆盖
   │
   ▼
把这次抓取的时刻写进主文件
```

一次抓取只做三件事：**抓、并、记**。它不判断、不打分、不推送、**也不管下次该从哪续**。

**丢掉的东西一律不留痕**：不入库、不计帖数、不写占位标记。视频帖与置顶帖只记进**已完成记录**，下轮不再为它们取一次详情页。

---

## 01§2 三个时间

| 时间 | 是什么 |
|:---|:---|
| **起始时间** | **参数 `begin_date`**：往回翻到哪。**由调用环节算好传进来**，抓取不自己判断 |
| **抓取截止** | **字段 `scrape_time`**：这次抓取**发生的时刻**，写进主文件 |
| **重叠余量** | **规则**：续抓时退到抓取截止**所在那一日的零点**（§5） |

- 起始时间**既决定翻页什么时候停，也决定文件里留下什么**（§4）
- 抓取截止是**抓取那一刻的挂钟时间**（形如 `2026-09-08 10:14:40`），**不是最后一条帖的发布时间**
- 抓取截止是**调用环节算下次起点的依据**（§5），也是**判断谁的数据不齐的依据**（§6）
- 重叠部分靠 `post_id` 去重，**多抓没有副作用**

---

## 01§3 四道关：什么条目才入库

列表翻回来的每一条，**四关全过才入库**。任何一关不过 → 丢掉，不入库、不计入帖数。

### 3.1 是本人发的

| 情形 | 处置 |
|:---|:---|
| 明确是本人在发 | 收 |
| 明确是他人 | 拒 |
| **认不出是谁** | **拒** |

**认不出身份的一律不收，不用任何旁证去补。**

> 博主长期停更时，头条的主页 feed 会用**跳转流**插入他人的帖子补位。不认身份，就会把别人的帖当成这位博主的新帖并进库，一路带到报告和推送里（2026-09-09 假帖事故即此）。

### 3.2 发帖时间是真实的

**只认 `publish_time`，退而取 `create_time`。两个都没有 → 时间视为未知，这条丢掉。**

**绝不用 `behot_time`。** 它是 feed 的**热度游标**，约等于抓取时刻；用它会把旧帖伪装成刚发的帖。

> 这三个是**头条接口的字段名**，不是我们存下来的名字。落库时统一存成 **`pub`**（§7）。

### 3.3 不是列表页噪声

形如「`博主名`：发布了一篇内容-今日头条」的条目是列表页占位条，**不是博主的帖子**，丢掉。

### 3.4 不是置顶帖

**置顶帖按列表条目上的置顶标记（`is_stick`）识别，丢掉不入库。**

置顶帖是博主**挂在主页顶上**的那一条，**可能有好几条**，日期可以是任何一天 —— 它是一条真帖，但它是**公告位**，不在时间线上：

- 它会**插在列表最前面**，把「按时间倒序翻页」这件事搅乱
- 它会让 `time_range.earliest` 变成几个月前，看着像中段漏抓
- 下游按时间做的任何判断，都会被它带偏

**与视频帖同样处置**：判定即丢，只记进**已完成记录**，永不重抓；下游看不见它。

---

## 01§4 抓取＝合并，永不覆盖

**翻页时每页全收，收完再统一过一遍起始时间。**

- **翻页全收**：一页 12 条**整页收下**，不逐条判边界；停不停只看「本页**最新的一条**是否已早于起始时间」（§2）
- **收完统一过滤**：把这一轮收下来的帖**整批过一遍**，`pub` 早于起始时间的丢掉
- **过滤在取详情页之前做** —— 反正要丢，就不必为它们再取一次全文
- 因此**边界那一页上早于起始时间的几条帖会一起丢掉** —— 这是有意的，不是漏洞

> 不逐条判、要整批过，是因为翻页是**倒序**的：逐条判就得在页中间做取舍，而页与页的交界处本来就没有干净的切点。

- 过滤后的帖按 `post_id` 去重后**并入**主文件；**原有的帖原样保留**
- **过滤只作用于这一轮收下来的帖，不作用于文件里已有的帖** —— 已经把历史带进来的博主，不会因为换了起始时间就被削掉
- 主文件的帖子数**只增不减**（除非博主自己删帖）
- 因此不存在「强制覆盖」这个开关
- 抓取**幂等**：同一位博主、同一个区间抓两次，结果完全一样

> 传一个很短的起始时间（如 `2026-09-01`）**不会**删掉历史；它只决定**这一轮收进来的**有哪些。

---

## 01§5 续抓

```
新起始时间 = 该博主 scrape_time 所在那一日的零点
```

举例：`scrape_time` 是 `2026-09-08 10:14:40` → 新起始时间取 `2026-09-08 00:00`。

- **这条规则由调用环节执行** —— 它读 `scrape_time`、算好起点、再调抓取。抓取自己不读这个字段
- 没有抓取记录的博主（新博主），退回用固定的起始时间
- 每位博主**各记各的**，互不影响。**不存在一份全局的「抓到哪了」**
- **抓取与解析共用这一个起点**：这一轮抓回来什么区间，就解析什么区间

---

## 01§6 对齐与补全

**对齐** = 把所有人的 `scrape_time` 放在一起，比**距现在多久**。有一个**只读的查看命令**打印总览，**它不抓取、不修改任何东西**。

```
博主              起始         抓取截止            帖数   距今
张大坤            2026-01-01   2026-09-08 10:14   2191   4 天
白猫财眼          2026-01-01   2026-09-10 18:40   1847   2 天
稀豹              2026-01-01   2026-08-28 09:12   1582   15 天
```

- 总览只显示时间差，**不划落后线**
- **补齐是手动动作，不自动触发**：把落后的博主**从各自的 `scrape_time` 补到今天**
- 补齐的起点就是续抓的起点（§5）。**补齐环节负责算这个起点**，算好再逐个调抓取 —— 抓取自己不看 `scrape_time`
- 补齐能**点名**补，也能**一把扫全部**（两者是同一个动作，只差有没有传博主）

---

## 01§7 产物：主文件记什么

- 文件名**永远是** `<博主名>.json`，**不含时间**；`data/posts/` 下**只放这个形态的文件**

| 顶层字段 | 是什么 |
|:---|:---|
| `user_info` | `{name, user_id, description}` —— 博主本人 |
| `source_url` | 抓取入口地址 |
| `scrape_time` | **抓取截止**，形如 `2026-09-08 10:14:40` |
| `stop_reason` | 翻页为什么停，只有四种取值：`since`（碰到起始时间了）／`no_more`（feed 真的翻到底了）／`capped`（翻到页数上限或条数收够，**我们自己截断的**）／`error`（接口异常中断） |
| `time_range` | `{earliest, latest}` —— 帖子的**发布时间**跨度，是内容的属性 |
| `posts` | 帖子数组 |

| 每条帖的字段 | 是什么 |
|:---|:---|
| `post_id` | 头条的帖子编号，**去重与合并的唯一钥匙** |
| `title` | 帖子标题；**只有文章类帖有**（见 §8） |
| `content` | 帖子的文字全文，**每条都有** |
| `pub` | 发帖时刻，形如 `2026-08-28 15:37`，**精确到分钟** |
| `url` | 帖子链接 |
| `digg_count` / `comment_count` / `read_count` | 点赞数 / 评论数 / 阅读数 |

样例（字段已截断）：

```json
{
  "user_info": {"name": "张大坤", "user_id": "1308872855797657", "description": "天下大势，浩浩汤汤…"},
  "source_url": "https://www.toutiao.com/c/user/token/Ciz3Ytq…",
  "scrape_time": "2026-09-08 10:14:40",
  "stop_reason": "since",
  "time_range": {"earliest": "2025-12-31", "latest": "2026-09-08"},
  "posts": [
    {
      "post_id": "1874751803693056",
      "content": "缩量横盘别慌，九月行情可期！\n\n收盘啦！今天指数全天窄幅震荡，最终微跌0.11%…",
      "pub": "2026-08-28 15:37",
      "url": "https://m.toutiao.com/w/1874751803693056/",
      "digg_count": 12, "comment_count": 0, "read_count": 1703
    }
  ]
}
```

---

## 01§8 标题、正文与视频帖

**两个文字字段各管各的，在一条命令内部一次拿齐，不分步。**

| 字段 | 是什么 | 从哪来 |
|:---|:---|:---|
| `title` | 帖子标题 | **列表**接口，翻列表时就有；文章类帖有，微头条没有 |
| `content` | 帖子全文 | **详情页**，逐帖再取一次 |

- `content` **必填**：取不到详情页时，退回用列表给的文字
- `title` **可选**：微头条没有标题，此字段不出现
- **标题文字不在正文里**：全库有标题的帖中，85% 的标题文字在正文里根本找不到。所以 `title` 丢了就是永久丢了

**微头条**（链接形如 `m.toutiao.com/w/…`）**不走详情页** —— 列表给的文字**就是它的全文**，直接落进 `content`。只花一次便宜的 info 接口请求做视频帖判定。

**文章类帖要逐级降级取详情页**：移动端 info 接口 → 移动端页面渲染 → 把页面里的段落拼起来。三条都不通（风控页，文案含「当前网络环境无法查看」）→ **这条帖算没抓到**，重试。

**视频帖：不入库**

- **四条判据，任一命中即判定**：info 接口返回**视频播放凭证**／页面 HTML 里出现**播放器标记**／文字是「**视频加载中**」／**链接域名是 `toutiaoimg`**
- 一经判定**直接丢掉**，与置顶帖同样处置（§3.4）—— 不写进主文件、不计入帖数、`content` 不留占位标记
- 记进**已完成记录**，**永不重抓** —— 否则每轮抓取都要为它再取一次详情页
- **下游看不见视频帖**：它在入库前就被扔了，报告（03）与推送（06）都不必再判一次

---

## 01§9 抓完必须校验

抓取完成后、在把数据交给任何下游之前，**必须**跑一次完整性校验。

| 类别 | 含义 | 例子 |
|:---|:---|:---|
| **硬失败** | 该抓到的区间没抓全 | `post_id` 重复、关键字段为空、一条帖及其发帖时间都没有、翻页被上限截断、接口异常中断 |
| **仅提示** | 可能完全正常 | 博主停更造成的日期空档、帖子数变少、没碰到起始时间就翻完了 |

「该抓到的区间没抓到」**看停止原因本身**（§4）：

| `stop_reason` | 结论 |
|:---|:---|
| `since` | **覆盖到位** —— 它只在「本页最新的一条都已早于起始时间」时才盖，而页面按时间倒序、首尾相接，所以盖了 `since` 就等于「≥ 起始时间的帖一条不落」 |
| `no_more` | **仅提示** —— feed 真的翻到底了。起始时间之前 feed 里确实没有更早的帖 |
| `capped` | **硬失败** —— 翻页被自己设的上限截断。还拿得到更早的帖，只是没拿，调高上限后重抓 |
| `error` | **硬失败** —— 接口重试三次仍失败。这一轮的区间没收全，重抓一次 |
| 文件里最早的一条正好落在起始时间当天 | **覆盖到位** —— 本身就是铁证，不必再看停止原因 |

- 「帖子数变少」永远是**仅提示**
- 校验**报出问题不等于抓取失败** —— 它只负责说清楚哪里可疑

---

## 01§10 怎么跑

```bash
# 抓取 —— 给链接、给起点。抓什么区间，下游就解析什么区间
python -m blogger.scrape <帖子链接> --begin_date 2026-01-01

# 只看不抓：打印对齐总览（谁的数据不齐）
#   尚未实现

# 补齐（手动）：读总览里落后的那些博主，各自算好起点，逐个调上面的抓取
#   尚未实现（不传博主名 = 全库；传博主名 = 只补这一位）

# 抓完校验（必做）—— 传**与抓取同一个**起始时间，否则覆盖检查判不了
python -m blogger.scrape.verify <博主名> --begin_date 2026-01-01
```

**抓取命令只有 `begin_date` 一个参数** —— 所有「该从哪续」的判断都在补齐环节，抓取本身不碰。

抓帖只有**一条命令**，标题与正文在内部一起取 —— 没有分步调的第二道工序。

推送那一档的**增量**由推送流程内部调用，不是手动跑的命令。

---

## 改动记录

| 日期 | 改了什么 |
|:---|:---|
| 2026-09-12 | 定案：`stop_reason` **加第三种 `error`** —— 接口重试三次仍失败单独盖章，不再混在 `no_more` 里冒充正常收工。**只有正常收工（`since`／`no_more`）才推进抓取截止**，否则下次从今天续、把没抓到的中段永久跳过 |
| 2026-09-12 | 定案：`stop_reason` **再加第四种 `capped`** —— 翻到页数上限／条数收够，是我们自己截断的，`no_more` 只留「feed 真的翻到底了」。两种都不推进抓取截止。§9 判 `capped` 为**硬失败**（还拿得到更早的帖，只是没拿） |
| 2026-09-12 | §9 覆盖判据改为**只看停止原因本身**：`since` = 覆盖到位、`no_more` = 仅提示、最早一条正好落在起始时间当天 = 覆盖到位。**删掉折天数复核**「一页以上的量」—— 那个量折算成几天没有可执行的算法，遇周末与长假必然假报硬失败 |
| 2026-09-12 | ~~§3.2 `stop_reason` 的 `no_more` 释义改成「没碰到起始时间就停了」~~ —— **同日推翻**：实测它盖着三件事，正确的修法不是改释义而是**拆章**（`capped` / `error`），`no_more` 回归「feed 真的翻到底了」。见本表首两行 |
| 2026-09-12 | 定案：**起始时间统一叫 `begin_date`**，只有这一个参数（删掉试跑用的月份换算法）。抓取与校验**传同一个值**，覆盖检查才判得了 |
| 2026-09-12 | 定案：**起始时间是硬边界** —— 早于它的帖一律不入库。**翻页每页全收、收完统一过滤一遍**（不逐条判边界），因此边界页上那几条早于起始时间的帖会一起丢掉。**过滤只作用于这一轮收下来的帖**，不削历史。§0 补一句：这一层做干净了，下游不必再筛 |
| 2026-09-12 | 定案：**置顶帖不入库**，按列表条目上的置顶标记识别 —— **可能有好几条**，日期任意。理由：它是公告位、插在列表最前面，搅乱倒序翻页，并让 `time_range.earliest` 失真。与视频帖同样处置（判定即丢、只记已完成记录） |
| 2026-09-12 | §1 一图流改为「四道关」；丢掉的东西一律**不留痕**（不入库、不计帖数、不写占位）统一写在 §1 末尾 —— 此前 §1 说视频帖「只写占位标记」，与 §8「不留占位」自相矛盾 |
| 2026-09-12 | §1 一图流把「收完统一过滤」提到「取详情页」**之前** —— §4 原本就写的是「翻页时每页全收，收完再统一过一遍」，一图流的顺序与它不一致。反正要丢的帖，不必为它再取一次全文 |
| 2026-09-12 | §3.4 把置顶标记**指名成 `is_stick`** —— 与 §3.2 指名 `publish_time` / `behot_time` 体例一致。实测：只有置顶的那条带这个键，别的条目压根没有 |
| 2026-09-12 | §8 补：**微头条不走详情页**，列表给的文字就是全文；逐级降级只适用于**文章类帖** |
| 2026-09-12 | ~~§9 硬失败判据改为「`stop_reason` 说是抓到边界了、但最早的一条离起始时间还差一页以上的量」~~ —— **同日推翻**，见本表首行。过滤之后文件里不可能再有越界帖这一点仍成立（旧判据「最早的一条晚于起始时间」确实会误报） |
| 2026-09-12 | **立稿**（新结构下的 01，取代 `legacy/06-抓取.md`）。新增 `§3 三道关`：身份 / 时间诚实 / 列表页噪声 / 文字够长 —— 身份关与时间诚实关旧文档完全没写 |
| 2026-09-12 | 定案：字段名写实名（`scrape_time` / `title` / `content` / `publish_time` / `stop_reason` / `time_range` / `user_info`） |
| 2026-09-12 | 定案：身份关**认不出就拒**，不用历史重叠背书；**删 `total_posts`**（等于 `len(posts)`，实测 2/162 个文件对不上）；~~`stop_reason` 只留 `since` / `no_more` 两种~~ —— **同日改成四种**，见本表首两行 |
| 2026-09-12 | 定案：微头条**没有标题**，列表文字落进 `content`；`content` 必填、`title` 可选（文章类帖才有）—— 旧文档「微头条正文留空」写反了 |
| 2026-09-12 | 定案：**取消「补正文」分步工序**，标题与正文在抓取命令内部一次拿齐；`title` 来自列表、`content` 来自详情页 |
| 2026-09-12 | 定案：视频帖识别**加链接域名 `toutiaoimg` 作第四条判据**（旧文档待定 #41） |
| 2026-09-12 | 输入明确为**博主的任意一条帖子链接**；`data/posts/` 下只放 `<博主名>.json` |
| 2026-09-12 | §6 定案：**补齐是手动动作、不自动触发**，起点就是续抓起点，删掉「多远算远」的判定；§0 补「抓取从不自己发生」 |
| 2026-09-12 | 待补定案：认不出身份的量**暂不改**；风控（限速/重试/间隔）留到写代码时设计，硬要求是不能被封；旧数据**全库重抓**，残留分片随重抓消失 |
| 2026-09-12 | 定案：**视频帖不入库** —— 判定即丢，不写主文件、不计帖数、不留 `[视频帖]` 占位。此前是「照常存进库里、由下游决定跳过」。下游因此不必再判一次 |
| 2026-09-12 | 定案：**删掉「文字够长（不足 5 字丢掉）」这一关** —— 三道关就是三道，与 §1 一图流一致 |
| 2026-09-12 | 定案：**删掉 §1 一图流末步「扫一遍 scrape_time 自动补一段」** —— 与 §6「补齐是手动动作、不自动触发」矛盾 |
| 2026-09-12 | 定案：**抓取是区间式的** —— 抓 `[起始时间, 至今]`，**抓多少就解析多少**。起始时间一律由**调用环节**算好传进来，抓取自己不读 `scrape_time`、不判断该抓多少 |
| 2026-09-12 | 定案：**新增 `align_posts` 补齐命令** —— 「该从哪续」的判断从抓取脚本移到补齐环节；抓取只剩 `--since` 一个参数，原来的 `--align` 开关取消 |
| 2026-09-12 | 定案：**发帖时间字段统一成 `pub`** —— 帖子里的 `publish_time`（Unix 秒）与 `publish_date`（字符串）合并为一个 `pub`。`publish_time` / `create_time` / `behot_time` 是**头条接口的字段名**，不是我们存的名字 |

## 待补

| # | 待定的事 |
|:---|:---|
| 1 | **认不出身份的条目有多少**：§3.1 定的是「认不出就拒」。写代码时留意这个口径会拦掉多少条（尤其 feed 本身不带 user 的博主），量出来后再看要不要留后门。**暂不改** |
| 2 | **风控**：§8 的详情页重试，以及整条抓取链的请求节奏 —— **限速、重试上限、间隔**都留到写代码时设计。硬要求是**不能被封** |
| 3 | **旧数据全部重抓**：`data/posts/` 里 9 个 `_bodies_s*.json` 分片是旧「补正文」工序的残留，与 §7「只放 `<博主名>.json`」不符。计划是**测试通过后全库重抓**，残留随重抓一并消失 —— 不单独清理 |
| 4 | **补齐命令的名字待确认**：`align_posts` 是我起的名（§10）。它不抓取，只负责算出每位的起点再逐个调抓取 |
| 5 | **旧帖里的 `publish_time` / `publish_date` 怎么迁**：§7 已定成 `pub` 一个字段。存量 172 个文件都是旧形态 —— 若走「全库重抓」（待补 #3），随重抓自然消失；否则要单独迁一次 |
