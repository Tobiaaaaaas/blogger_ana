# 一次性任务清单

以下任务只需执行一次（或按需低频重跑），每次完成后在这里记录执行时间与结果。运行后 `profiles.json` 会自带 `updated` 时间戳作为第二重记录。

| 状态 | 任务 | 命令 | 执行时间 | 备注 |
|---|---|---|---|---|
| ✅ 已完成 | 生成博主画像档案（Mac） | `python -m briefing.scripts.profiles` | 2026-09-01 21:12 | 覆盖全部 90 位博主（非仅追踪 20 人），写入 `data/briefing/profiles.json`；**增量逻辑**：已画像博主跳过，只处理新增博主 |
| ✅ 已完成 | 生成博主画像档案（Windows） | `python -m briefing.scripts.profiles` | 2026-09-01 21:45 | Windows 侧重新生成（90 位，`C:\Users\24966\blogger_ana\briefing\data\profiles.json`），供长期运行自足 |
| ✅ 已完成 | 建飞书群机器人并填入 webhook | 手动 | 2026-09-01 21:35 | webhook 已写入 `briefing/.env`（gitignored），Mac 实测发卡成功（`code:0`） |
| ✅ 已完成 | TRACKED 刷新为综合口径 t 值 top-20 | `comparison_all.py` + `config.py` | 2026-09-01 | 总榜改综合口径（显式+nd 合并）按 t 值排序、资格放宽到综合均分>0；TRACKED = 新 top-20（掉出 刘海娃娃/乐哥来了/股傲，新进 强哥解盘/时间轨迹/江河之水终有入海之日） |
| ✅ 已完成 | Windows 代码同步（补缺失脚本） | tar（scripts/ + briefing/scripts/ + runners/） | 2026-09-01 23:1x | Windows 缺 `scripts/utils/fetch_bodies_shard.py`（正文补抓脚本，旧部署遗漏）→ 导致新博主 body-fetch code=2；已补传 + 全代码树同步，实测 fetch_bodies_shard 正常退出 |
| ✅ 已完成 | TRACKED 扩为 top-30 | `comparison_all.py` + `config.py` | 2026-09-01 | 总榜综合口径 t 值前 30（新增 刘海娃娃/TL阳光/股评老陈/财牛/谭阿坤/波段研究师/时空鹰眼/实盘指龙副号/柚来又去/鸟瞰股市；#30 鸟瞰股市 t=+0.46 与 #31 梦幻之歌并列，按稳定排序取鸟瞰股市）；config.py 已同步 Windows |
| ✅ 已完成 | 首轮真实推送（过去 2 日观点） | `--push --slot evening` | 2026-09-01 23:34 | 重置 state.json 触发 48h 首期窗口（since=08-30 23:19）；30 位博主全量抓取（[1/30]→[30/30]，40 条新帖 / 7 位博主发帖）、抽点 8 条、共识偏多（2多/0空）；**飞书推送成功 code:0**，历史 `20260901_233403_晚间.json`；state 已入增量（last_run=23:19、seen 7 博主）。⚠️ 实盘指龙副号 抓取失败（feed 主体与账号名不一致，疑抓错账号）；⚠️ 机器 23:34 后 sshd 间歇卡顿数小时，09:15/10:00 晨间推送被 Task Scheduler 跳过，11:00 起各档已恢复正常（机器现已稳定） |
| ✅ 已完成 | 抽点批覆盖 bug 修复 + 首报全名单 | `summarize.py` | 2026-09-02 | `extract_points()` 用批内索引 `j` 当 key，后一批覆盖前一批（首报 40 帖只留 8 条）→ 改全局索引 `i+j`；`MAX_KEY_BLOGGERS` 5→30。本地模拟 40 帖→40 点全保留（PASS） |
| ✅ 已完成 | 首报重构：每博主最新观点（无窗口限制） | `run_briefing.py` | 2026-09-02 | `_collect_firstrun_views()`：直接读主文件全量历史，每博主取最近一篇有实质内容（非视频帖）的观点帖，`FIRST_RUN_MAX_AGE_DAYS=7` 时效把关（超龄视为无近期观点）；`FIRST_RUN_FETCH_DAYS=7` 作爬虫刷新窗口。之后严格增量（since=last_run）。首报 dry-run 29/30 博主取到近期观点（仅 实盘指龙副号 因 25.7 天超龄排除） |
| ✅ 已完成 | 综合输出容错（拍平/缺段重试） | `summarize.py` | 2026-09-02 | 模型偶发把 consensus 拍平到顶层（`{stance,bull,bear,neutral,summary}` 无 consensus 键）→ `_normalize_synth_result()` 包回 + `_synth_result_ok()` 完整性检查 + `SYNTH_MAX_ATTEMPTS=3` 重试；另 `_overwrite_counts()` 用抽点权威值覆盖 LLM 漏计的多空/中性数（观测 10 中性被计成 1） |
| ✅ 已完成 | bat 双开修复 + ASCII/CRLF 硬化 | `briefing_runner.bat` | 2026-09-02 | 根因①：`>> briefing.log 2>&1` cmd 手柄与 python FileHandler 双开同文件 → PermissionError 退出 1；已删重定向、加面包屑 echo。根因②：bat 曾为 UTF-8 中文 + LF 行尾，cmd 按 GBK 解析把 `rem`/`set` 行拆成乱命令（面包屑静默丢失）→ 改为**纯 ASCII + CRLF**，实测 `cmd /c bat testxyz` 无乱命令错误、面包屑 `TRIGGER slot=` 落盘 |
| ✅ 已完成 | 定时任务重建 | PowerShell `Register-ScheduledTask` | 2026-09-02 | 7 档（BriefingEarly/Am/Am2/PmPre/Pm/Close/Evening）重建为 24966 身份 + `StartWhenAvailable` + `ExecutionTimeLimit 60min`（ssh 非提权无法 `/ru SYSTEM`）；11:00 自然触发验证 Last Result=0、锁跳过正常、面包屑待确认（旧 UTF-8 bat 未写面包屑，修复后已写入） |
| ✅ 已完成 | 卡片重排版：重点博主按总榜排名挑 top-8 | `render.py` | 2026-09-02 | 用户反馈"挑重点（排名高的博主）+ 排版太满不可读"。`config.TRACKED` 顺序即综合口径 t 值总榜（top-30 快照），`TRACKED.index(name)+1` 即排名。新增 `select_key_bloggers()`（按排名排序截 top-8）+ `_fmt_key_blogger()`（单行 `#n **名字** 🔴看多·强·近几日｜“quote”`，去掉 ▸ 点评行）；`_previous_from_card`/`_preview_text` 复用同一选择（上期基准=实际展示）。**已推送**：从 `20260902_112802_上午盘中.json` 重构 card → 新布局重渲染 → 飞书 code:0，历史 `20260902_113948_上午盘中_重排版.json`；零重抓、零 LLM 调用、零状态改动 |
| ✅ 已完成 | **v2 全板观点模型 + 重点博主两行式（Top 5 + 帖子时间 + 画像风格）** | `state.py`/`summarize.py`/`render.py`/`run_briefing.py` | 2026-09-02 | 用户确认：重点博主 Top 5、两行式（第一行 `#n 🟢 **名** 看空·强·近几日｜“quote”（时间）`，第二行缩进画像档案 style）、引文后括号标帖子时间（今日HH:MM/昨日HH:MM/MM-DD HH:MM）、中性博主不进重点（仅计全板）。**全板观点模型**：state 增 `recent_views`（每博主存一"近期观点"=最新帖立场，发新帖即更新，没发帖保持不变）；共识多空统计=对全部博主近期观点计数（非增量帖统计）；7 天时效，超期博主退出统计；分歧/风险/要点基于全板。综合 prompt 改全板输入（`synthesize(board, updated, ...)`，不再输出 key_bloggers/多空数字）。**已推送**：Windows 首期建板 29 位（7多/14空/8中性），飞书 code:0，历史 `20260902_115550_午前盘中.json`；state 已提交全板（last_run=11:54，旧 state 备份 `state.bak_v1`），下档 12:45 起自动增量 |
| ⏸️ 搁置 | **雪球博主接入（可爬性已确认，实施待用户决策）** | `docs/xueqiu_crawl_research.md` | 2026-09-02 | 调研结论：**能爬**（Playwright 解阿里云 WAF + 浏览器内 `v4/statuses/user_timeline.json`，字段比头条更全，昵称→id 用 `query/v1/search/user.json`），但**匿名限流极严**（3 秒间隔 15 连调挂 12 次，等 30 秒可恢复），需登录态。用户决策：登录方案/接入范围/博主名单全部搁置，之后再做。方案骨架（对标 scrape_toutiao.py 写 scrape_xueqiu.py，产出同构 `data/posts/<博主>.json`）已写入留档 |
| ✅ 已完成 | **v4 简报改版：内容加强（风格/观点/共识/分歧→关注点）+ 博主两行展示（名+风格/观点/空行）+ 大结构退回** | `summarize.py`/`render.py`/`run_briefing.py` | 2026-09-02 | 用户反馈：大结构退回 v2（关注点**回底部**，不做置顶）；"博主观点的总结应兼顾博主风格/观点/共识/分歧，总结出该关注的若干点"+飞书可读性。**内容加强（prompt）**：`takeaways` → `focus`（3~5 条，每条 `{theme(≤8字), detail(≤50字点名博主+风格+维度), action(≤20字行动)}`）；分歧/风险条目带博主画像风格。**博主两行展示（render）**：`#n 🟢 **名** · 画像风格` + 换行 `看空·强·近几日｜“quote”（时间）`，博主间**空行隔开**（join `\n\n`，payload 与 `_preview_text` 一致）。修复 `_previous_from_card` "较上期：较上期…"重复。**已推送**：Windows 首期建板 29 位（7多/14空/8中性），飞书 code:0，历史 `20260902_123454_上午盘中.json`；state 已提交（last_run=12:33，旧 state 备份 `state.bak_v3`），下档 12:45 起自动增量 |
| ✅ 已完成 | **v5 共识恢复丰富段落（"分析太少"回炉）** | `summarize.py`/`render.py`/`run_briefing.py` | 2026-09-02 | 用户反馈："你看之前的，非常丰富的分析，现在的分析太少了" + "之前的版本+当前最新的重点博主观点的排版+加强总结分析"。**共识**：把 v4 的 `summary`（一句话≤40字）+`evolution`（演变行）两行结构**改回 v2 式丰富分析段落**——`consensus.summary` 以"较上期……"开头（4~7 句，覆盖 ①时段背景 ②多空力量对比与占优方 ③本期更新博主带来的变化-点名立场 ④中性观望 ⑤风险偏好），首期写"较上期：首期无基准"；evolution 字段从 schema 删除（旧输出仍容忍）。`_sanitize_card` summary 截断 `[:60]`→`[:400]`；render 共识渲染去掉 `· ` 前缀恢复独立段落行；`_previous_from_card` 去掉 evolution 拼接、上期基准截断放宽 `[:300]`→`[:600]`；`_preview_text` 同步。**保留**：博主两行展示（名+风格/观点/空行）、关注点（底部）、大结构不变。**本地 dry-run 验证**（新 prompt 产出 6 句丰富共识，点名双方阵营+中性+风险偏好）。**已推送**：Windows 重置 state（备份 `state.bak_v4`）触发首期重建 → 飞书 code:0，共识=偏空（7多/14空/8中性，与 v4 计数一致），历史 `20260902_124429_午后盘前.json`；state 已提交（last_run=12:43、last_slot=pm_pre、recent_views=29），下档 14:00 起自动增量 |
| ✅ 已完成 | **v6 本期要点改收尾总结（关注点"太杂"回炉）** | `summarize.py`/`render.py`/`run_briefing.py` | 2026-09-02 | 用户反馈（贴 v2 示例）："最后的关注点太杂了，写成总结"——`🎯 本期要点\n· 全板偏空，空头占优，短期以防守为主` 式**单行总结句**，取代 v4/v5 的 ①主题/说明/→行动 三行式散碎条目。**prompt**：`focus`（{theme,detail,action}）→ `takeaways` 数组（3~5 条，通常 4，每条**一句完整总结句 ≤35字**，不拆分主题/说明/行动、不堆博主名风格；综合全板共识/对立/变化/点位/风险提炼收束）。**归一化**：`_norm_takeaways()` 收字符串（旧 dict 压成一句兼容），`_sanitize_card` 统一存 `takeaways` 并 `pop(focus)`；`_normalize_synth_result`/`_synth_result_ok` 同步。**render**：删除 `NUM_CIRCLED`/`_fmt_focus`，新增 `_fmt_takeaway`，末节改 `🎯 **本期要点**` + `· {句}`；`_preview_text` 同步（import `_fmt_takeaway`）。**部署插曲**：12:45 定时任务（旧代码在跑）与新代码推送撞锁 → 杀掉该进程（爬取中途无 state 写入，已入 data/posts 的新帖被首期重建纳入）；删除残留 run.lock。**已推送**：Windows 重置（备份 `state.bak_v5`）→ 首期重建 code:0，共识=偏空（10多/12空/7中性，纳入 11:40-12:19 新帖后更新），历史 `20260902_125823_午后盘前.json`；state 已提交（last_run=12:52、recent_views=29），下档 14:00 起自动增量 |
| ✅ 已完成 | **v7 博主抓取并行化（14:00 慢——扫描太慢提速）** | `scrape_merge.py`/`run_briefing.py` | 2026-09-02 | 用户反馈 14:00 档晚 10 分钟才推：每档先把 30 位追踪博主**串行**逐位重扫（~22s/位 ≈ 11 分钟）。根因：每博主 = 独立爬虫子进程（Playwright 无头浏览器，页间自带 sleep(3) 反爬节流），22s 多为浏览器启动/goto/networkidle 开销。**并行化**：验证博主间无共享文件（主文件/窗口/`_bodies_s*`/备份全按博主独立）→ `fetch_all_new_posts` 串行循环改 `ThreadPoolExecutor`，`_worker()` 每博主一线程跑独立子进程，`threading.Lock` 保护 results/errors；默认 `workers=5`（Windows 22 核/31.5G，无头浏览器 ~0.5G），`run_briefing` 新增 `--workers`（1=串行兜底）。**验证**：Mac 实测并行 4 博主 27.9s（串行 ~88s）；Windows 实测并行 3 博主 24.0s 无报错（since=当前时刻无新帖，未污染生产数据）。全名单 30 位预计 ~2.5-3 分钟 + 综合 ~1 分钟 → 15:00 起档到推 ~4 分钟。已部署 Windows（scrape_merge/run_briefing MD5 一致），下档 15:00 close 起生效 |
| ✅ 已完成 | **v9 推倒重设计：18 人窗口速览卡 · 交易日 3 推** | `config.py`/`calendar.py`/`state.py`/`summarize.py`/`render.py`/`run_briefing.py`/`market.py` | 2026-09-02 | 用户要求：推送名单改固定 18 人，重设计为"每交易日 3 推（09:30/13:00/14:30）一张 18 行速览卡"，展示每位博主**近 3 个交易日**最新方向观点（摘要 + 逐字原话带发帖时间；波段 2日+ 与超短 0-1日 都算），卡底 LLM 收敛总结。**已落地**：config `ROSTER`=18 固定序 + `SLOTS` 3 档；calendar 新增 `trading_days()`；state 去 v8 键（首跑 `_migrate_state_v2` 自动清）；summarize 新增 `extract_window_rows`（每档全量 LLM 重读、批 2 并行 3、失败降级占位）+ `summarize_window`（一段收敛总结，计数系统权威注入）；render 新增 `build_roster_card_payload`/`build_minimal_card_payload`（①–⑱ 固定 18 行，引文时间系统回填真 publish_time）；run_briefing 热路径重写（展示窗口滚动 3 交易日现读主文件 / 抓取增量 since last_run / 零方向日最小卡不调总结 LLM / `--skip-calendar`）。v8 旧共识/心跳代码全部标 LEGACY 保留不删。**验证**：`trading_days` 边界（09-02→[08-31,09-01,09-02]、周六 09-05→[09-02,09-03,09-04]）PASS；Mac dry-run（`--slot morning --no-scrape`，4 位窗口内有帖）出 18 行卡：2多/2空，逐字原话带昨日/今日真实时间戳，卡底收敛总结点名快照内博主，历史落盘、state 未动。抽查 4 条引文时间全部对到窗口内真实帖。顺带修复 market.py sina 兜底条目缺 `pct` 导致 `market_line` KeyError 的旧 bug（腾讯抖动→新浪兜底时整档会崩）；再修 **v7"并行"从未真正默认生效**：`--workers` argparse 默认 `None` → `fetch_all_new_posts` 视作 1（串行 ~22s/人，调度从不传该参 = 一直在串行）→ 默认改 5 路（机器 22 核/32G）。docs（README/DEPLOY/TASKS）已同步 3 推口径 |
| ✅ 已完成 | **v9 部署 Windows + 重建调度（3 任务）+ warm-up** | scp + PowerShell Register-ScheduledTask | 2026-09-02 22:5x | 同步 7 个 .py 到 `windows-server`（MD5 与 Mac 逐文件比对全等）；Task Scheduler 删旧 7 档（BriefingEarly/Am/Am2/PmPre/Pm/Close/Evening）建新 3 档 **BriefingMorning 09:30 / BriefingAfternoon 13:00 / BriefingLate 14:30**（bat 字节不变，Register-ScheduledTask 带 StartWhenAvailable + ExecutionTimeLimit 60min + MultipleInstances IgnoreNew，状态 Ready）。**warm-up**：`--push --slot morning` 真实推送 → 飞书 `code:0`（15 人在窗，6多/8空/观望1/无更新3），历史 `20260902_230017_早盘.json`；state 自动迁移成功（仅剩 last_run/last_slot/seen，v8 的 recent_views/board_prev/previous 已清，last_run=2026-09-02 22:58）。首次自然触发 = 次日 09:30。**插曲**：① Windows sshd 服务当时未运行（重启后停）→ 用户拉起来后继续；② schtasks /create 双层引号被 ssh 弄坏 → 改用 ps1 文件执行 |

| ✅ 已完成 | **slot 大小写 bug 修复 + 真实任务触发验证通过** | `run_briefing.py`/`register_tasks.ps1` | 2026-09-03 10:3x | **今早 09:30 首次自然触发失败（LastTaskResult=1）**。根因双层：① 09-02 注册任务时参数误用大写 `Morning/Afternoon/Late`，而 `config.SLOTS` 键是小写 → 旧代码 `_resolve_slot` 零容错直接 `SystemExit("未知 slot: Morning")`（日志仅 TRIGGER 面包屑、零 python 行）；② `.lower()` 容错补丁昨夜 scp 中途超时（exit 143），09:30 时 Windows 仍是旧代码。**修复**：`_resolve_slot` 加 `key=args.slot.strip().lower()`；新增可复用注册脚本 `briefing/runners/register_tasks.ps1`（纯 ASCII，照抄原设置 StartWhenAvailable+ExecutionTimeLimit 60min+IgnoreNew+24966 Interactive），以**小写参数**重建 3 任务。**验证**：Windows run_briefing.py MD5 与 Mac `67dbbf6e` 一致；`schtasks /run /tn BriefingMorning`（真实任务路径）→ Ready/result=0，python 全流程跑通：窗口 15/18、行情 上证 3961.66 +0.51%、行抽取 7多/7空/观望1/无更新3、**飞书 code:0**，历史 `20260903_103834_早盘.json`。**数据缺口**：Windows `data/posts` 缺 18 人中 `孙万林.json`（新博主，Mac 有 336 帖）→ 已 scp 补传，现 18 人主文件齐全，13:00 档起生效 |
| ⚠️ 遗留 | 三粒光 feed 主体与账号不一致 | `scrape_merge.py` | 2026-09-03 | 增量抓取报 "feed 主体用户与 三粒光 不一致（疑抓错账号）"；其窗口内可读帖仍来自已合并主文件（444 帖），不影响展示，仅该博主无法增量更新——待查账号配置 |
| ✅ 已完成 | **v10 按周期分层：废除跨周期假"多空对立"** | `config.py`/`summarize.py`/`render.py`/`run_briefing.py` | 2026-09-03 | 用户反馈：多博主「超短·今日看反弹」+ 其他「波段·看空」**并不矛盾**，合并计数 7:7 + 总结点名"多空对立"是误读（反弹=波段调整中的反抽，层叠不冲突）。**改动**：①计数改分档 `count_rows`（`short/swing` 各多空 + 观望 + 无更新），文案源唯一 `config.format_counts`（角标/预览/总结兜底共用）；②`SUMMARY_SYSTEM_PROMPT` 重写为周期分层口径——开场用分档计数、跨周期反向一律写"层叠兼容"禁"对立/对峙/拉锯/阵营X比X"、分歧只限同周期真反向、反弹共识下的操作分化不算对立、结尾超短/波段各一句操作；③卡主体 18 行**按周期分两段**渲染（`_roster_section_lines`，⏱️超短/🌊波段/📭无方向，段内 ROSTER 原序、编号①–⑱=原单位次不变，行内去重复周期标签留 今日/明日/本周 词），payload 与 dry-run 预览共用同一块生成防漂移。**验证**：09-03 Windows 真实卡重建喂 LLM → 输出"两层方向相反但层叠兼容：超短反弹更像波段调整中的反抽，而非反转信号"；dry-run 三段标题 + 分档角标正确；已同步 4 .py 到 Windows（MD5 全等），真实触发 BriefingLate → result=0、飞书 code:0，历史 `20260903_153443_尾盘.json`（超短 5多/3空 · 波段 1多/4空 · 观望3 · 无更新2）。另证实当天 13:00 / 14:30 两档**纯自动触发**（非手动）均 result=0 + code:0 |
| ✅ 已完成 | **v11 部署 Windows + 真实触发验证** | scp + `schtasks /run` | 2026-09-03 16:1x | 同步 4 个 .py（config/summarize/render/run_briefing，MD5 与 Mac 逐文件全等）；核对 30 位面板博主 `data/posts` 齐全（仅 **香满衣** 缺失 → 已 scp 主文件，现 30/30）。Windows dry-run（live 数据，短 3多/6空·9/17、波 6多/12空·18/21）确认双板块结构与层叠总结正常；`schtasks /run /tn BriefingLate` 真实任务路径 → **result=0** + **飞书 code:0**，历史 `20260903_161859_尾盘.json`：**短 5多/6空（11/17 表态）· 波 5多/13空（18/21 表态）**，云帆观市 短看多·明天 + 波看空·本周 双行并存（跨周期分层自洽点出），同板块内真反向才写"分歧"；卡尾总结无跨板块"对立/对峙"措辞。下个交易日（09-04）09:30 起自动以 v11 推送 |
| ✅ 已完成 | **v11 双固定板块（超短17/波段21）+ 跨板块层叠总结收尾** | `config.py`/`summarize.py`/`render.py`/`run_briefing.py` | 2026-09-03 | 用户要求：一张卡设两个固定板块、核心观点总结在最后——超短板块看近 3 个交易日（今天/明天表态）、波段板块看近 7 自然日（近日/本周/下周/更长表态）。**根因**：v10 每博主一行、只按 LLM 判的最新周期塞一段，无法表达同一人同时持超短+波段两档（孙万林 09-02：超短"明天还有一次大跌做超短"＋波段"反弹见顶底部3800"）。**改动**：名单 = `reports/top20_值得关注博主.md` 双榜；`config` 改 `PANEL_SHORT`(17)/`PANEL_SWING`(21)/`ALL_BLOGGERS`(30 去重抓取)/`BOARD_META`/`format_board_counts`（删 ROSTER/BUCKET_*）；前 8 位双板块博主两块都上榜、按各自板块窗口与口径**独立抽取两次**；`summarize` 换两个按板块 prompt（短只认 今天/明天、波段只认 近日/本周/下周/更长/结构性中期，越界 has_view=false 不显示不计数；主题须落大盘/指数、行业板块自身趋势不算；summary 只写本板块层）+ `extract_board_rows`/`board_counts`（shown/members 无合计断言）+ `summarize_boards`（两板块=两层、跨板块反向=层叠禁对立、同板块真反向才叫分歧、清一色同向别硬凑分歧字样、结尾超短/波段操作句 ≤280字，失败兜底计数行）；`render` 板块版（计数头行 `⏱️ 超短(0-1日) · x多/y空（n/17 表态）` + 名单序行**无编号** + 空板提示行，payload/preview 同一块生成）；`run_briefing` 双窗口编排（short 近3交易日/swing 近7自然日，各自读帖抽取，共享 8 位各读各的窗口）。强哥解盘、知情达理星空hnR 退出卡片。**验证**：合成 rows 断言（分板块计数/名单序/空板/双板块博主两块立场独立/chunk 切分/无①–⑱编号）PASS；Mac 真实 dry-run ×3（短 2/17·波 16-17/21 表态）——孙万林 短看空·明天 + 波看空·更长 两行并存、卡尾总结点名跨周期分层自洽且无跨板块"对立"措辞；四十二流光（纯房地产持仓帖无大盘观点）经主题收紧后正确排除（见下行 Windows 真实触发）。 |
| ✅ 已完成 | **v12 日期锚定：相对词按发帖日换算绝对目标，过期表态自动隐去** | `calendar.py`/`summarize.py`/`render.py`/`run_briefing.py` | 2026-09-03 | 用户反馈：行头/摘要里 今天、明天、"昨天的明天" 太混乱，应以日期为准。**口径确认（4 问）**：①超短只留 目标日=卡片当天或下一交易日（已兑现的"昨天说的今天/明天"自动不显示）；②超短行头=**纯日期**（`看空 · 09-03`）；③引文时间统一**绝对 MM-DD HH:MM**（弃 今日/昨日 相对速览）；④波段 本周/下周 按发帖日锚定周一~周五周、目标周已过剔除，行头=周词+周日期段（`本周 08-31~09-04`）。**落地**：calendar 增 `next_trading_day()`（"明天"跨周末→下一交易日）；summarize 增 `resolve_anchors()`——`_anchor_row`（超短 今天=发帖日/明天=下一交易日，只留∈{卡片日,下一交易日}；波段 `_week_monday` 锚周、目标周周五<卡片日剔除；行带 anchor）＋ `_strip_rel_time()` 系统侧剥离行摘要里的 今天/明天/本周 等相对时间词（保留 周X/周初 等锚定周内表述与数字日期，不靠模型自觉）；SHORT/SWING prompt 声明"帖内时间词以该帖发帖日为准"+摘要禁相对日词；SUMMARY prompt 收紧日期纪律（快照行带 anchor 与引文绝对时间，博主目标日只能照抄、禁自行用 明天/下周 重推），`summarize_boards` 增【日期锚点】输入；render `fmt_post_time` 绝对化、`_fmt_board_row` 印 anchor；run_briefing 抽取后接 `resolve_anchors` 再计数，版本串 v12。**验证**：合成断言（昨日帖"明天"→卡片日、09-01"今天"目标已过剔除、08-28"下周"→本周 08-31~09-04、08-24"本周"已过剔除、next_td(Fri)→Mon）PASS；Mac 真实 dry-run：孙万林 09-02"明天"锚成 **09-03（今天）**、超短板块只剩指向今/明者（过期者不入计数）、波段周锚齐全、收敛总结不再把孙万林重推成 09-04。Windows 同步 + 真实触发待验 |
| ✅ 已完成 | **v13 拆两群两卡（超短盘中 30 分网格 × 波段三档）+ 帖子增量复用** | `config.py`/`state.py`/`paths.py`/`summarize.py`/`render.py`/`run_briefing.py`/`runners/register_tasks.ps1`/`README.md`/`DEPLOY.md` | 2026-09-03 | 用户要求：超短/波段**分开来发送**（各自总结各自观点，不掺另一板块）、设计帖子保存/更新免重复抓抽；另给波段新群 webhook。**口径确认（7 问）**：超短盘中每 30 分一档（09:30…15:00 十档）→ 旧群；波段 09:30/11:00/14:30 三档 → 新群；窗口 超短近 **2 自然日**/波段近 **5 自然日**（v12 目标锚定过滤保留；已知周一早晨不含上周五"看周一"帖——用户接受）；每档照发整版（无 🆕）；本板块零表态发该板块最小卡。**落地**：config 退役 `SLOTS`/`slot_for` → `TRADING_TICKS`/`SWING_TICKS`/`in_intraday_grid`/`due_boards`/`board_title`/`WEBHOOK_ENV`/`format_board_count`（`WINDOW_CAL_DAYS={short:2,swing:5}`，`format_board_counts` LEGACY）；state 双水位 `fetched_at`（抓完即写）/`last_run`（推完才写）+ `_migrate_state_v3` 回填；行抽取缓存 `rows_cache.json`（指纹=`post_id:sha1(content)[:8]` 含正文——防 done-dict 正文回填命中过期缓存；无观点也缓存 null；LLM 失败不缓存；缓存行每档仍 `resolve_anchors` 重锚；`quote_ts≥窗界` 二道防御）；summarize 增单板块 `summarize_board`（拆 SHORT/SWING 两块 prompt，各自日期纪律收窄：short 锚=卡片日/下一交易日可写 今明、swing 锚=本周/下周周段、近日/更长不加日期；删全部跨板块层叠/两层对立措辞；≤240 字）旧跨板块 `summarize_boards` LEGACY；render 增 `webhook_for`（读 `WEBHOOK_ENV`，**返回 None=该板块失败，绝不回落旧群**）`build_board_card_payload`/`build_minimal_card_payload`/`build_board_error_payload`（单板块，旧双板块标 LEGACY）；run_briefing 增 `--time HH:MM`（覆盖决策 now）/`--board auto|short|swing|both`，`wall`(真实墙钟，状态/水位/历史名) vs `now`(决策) 分离，auto 门禁在**锁/抓取前** exit 0，逐板块 try/except 独立推各群，历史名 `{wall:%Y%m%d}_{HHMM}_{board_key}.json`（去冒号防 Windows 非法）；`register_tasks.ps1` 改**单任务 BriefingIntraday**（Daily 09:30 + Repetition 30min×6h，直达 `python.exe -m … --push` 绕过 bat，ExecutionTimeLimit 20min<30 分网格，StartWhenAvailable+IgnoreNew）。**Mac 验证**：门禁 off-grid（--time 12:00/09:00/15:30/10:13）exit 0 无副作用；09:30 `--board both` 双卡 dry-run（超短 1/17 · 波段 6/21，标题"超短盘中 09:30"/"波段早盘 09:30"，两总结各自独立无层叠措辞，孙万林 09-02"明天"→行头 **09-03** 锚定正确）；同档重跑 rows_cache **全命中**（短 5 人/波 13 人）仅剩每板块一次总结 LLM；swing webhook 置空 → 记失败不 HTTP 旧群 rc=1；真实 `--push` 双水位推进、同档重推幂等；单元断言（board_title/format_board_count/空板最小卡仅单板块计数/网格边界/due_boards/PANEL 名单序）PASS。**Windows 同步 + register_tasks + 真实触发验两群 待验**（下一交易日 09:30/10:00/11:00 分别应收 双卡/仅超短/双卡） |
| ✅ 已完成（Mac 验证通过；Windows 待验） | **v14 窗口改交易日口径：超短=前一交易日 00:00→now、波段=前 3 交易日→now** | `config.py`/`calendar.py`/`run_briefing.py`/`summarize.py`/`README.md`/`DEPLOY.md` | 2026-09-04 | 用户反馈：回看窗口改**交易日口径**（非自然日）——超短前 1 个交易日到现在、波段前 3 个交易日到现在；且"报告中有明确预测周期就写清楚（超短=今天或明天；波段可能不明确，不编造日期）"。**落地**：config `WINDOW_CAL_DAYS={short:2,swing:5}`（自然日）→ `WINDOW_TRADING_DAYS={short:1,swing:3}`（回看交易日数）+ `BOARD_META` 空板文案随窗口改；calendar 新增 `n_trading_days_ago(d,n)`（交易日回看起点，缓存二分 + 回退步进）；run_briefing `_window_start_ts/_window_txt` 改用交易日起点（前一交易日 00:00）→ **周一早晨窗口含上周五帖**（消除 v13 已知取舍）、卡覆盖文案改"前N个交易日到现在（MM-DD 起）"；summarize SHORT/SWING ROW prompt 与 docstring 窗口措辞同步；锚定/过期剔除逻辑不变（波段 未提/更长 仍不编造日期）。**Mac 验证**：`n_trading_days_ago` 边界断言（周一 09-07→上周五 09-04、跨春节）；dry-run 两板块窗口起点与覆盖文案正确（决策日 09-03 → 短 09-02 起 / 波 08-31 起）；py_compile 全过。**Windows 部署 + warm-up 双群 code:0 待验**（收盘后部署，次交易日 09-07 自然验收） |
| ✅ 已完成（Mac 验证通过；Windows 待验） | **v15 节奏统一：两板块同节奏 + 波段窗口 3→5 交易日 + 非交易日只推波段** | `config.py`/`run_briefing.py`/`runners/register_tasks.ps1`/`README.md`/`DEPLOY.md` | 2026-09-07 | 用户指示："最近波段和超短线的推送统一：交易日 9:00–15:00 每半小时 + 15:00–22:00 每小时；非交易日 9:00–21:00 每 3 小时；波段回看窗口 5 交易日"。**口径确认（4 问）**：同节奏仍两卡两群；交易日午休照发 + 09:00 盘前发；16:00 起整点（15:00 不重复）；非交易日**只推波段**；卡标题中性「板块+时刻」。**落地**：config `WINDOW_TRADING_DAYS={short:1,swing:5}`；删 `TRADING_TICKS`/`SWING_TICKS`/`_SWING_TICK_WORD`/`in_intraday_grid`，新增 `in_trading_grid`（09:00–15:00 :00/:30 ∪ 16:00–22:00 整点）与 `in_restday_grid`（09/12/15/18/21 :00）+ `due_boards(trading)`（交易日 [short,swing] / 非交易日 [swing]）；`board_title` 中性化；run_briefing `_resolve_boards` auto 分支按日型选节奏（非交易日不再 exit 0，命中 3h 网格推波段）；`register_tasks.ps1` 单任务 `BriefingIntraday` 拆 **BriefingDay**（Daily 09:00 + 30min×6h）+ **BriefingEvening**（Daily 16:00 + 1h×6h），日型/节奏由代码门禁区分；README/DEPLOY 同步（Linux cron 近似 + 双任务保真说明）。**Mac 验证**：节奏断言（交易日命中 09:00/12:00/15:00/22:00、离 15:30/08:30；非交易日命中 09/12/15/18/21、离 09:30/16:00）；due_boards 两分支；非交易日周六 12:00 auto→[swing]、10:00→exit 0；py_compile 全过。**Windows 同步 + 重注册双任务 待验** |
| ✅ 已完成 | **v16 后重构随 commit 记录（2026-09-08 起）** | git log | 2026-09-09 | 推送链自 09-08 起并入共享 `opinion/` 模块（prompts.py 标注契约单一同源、逐帖 rows_cache 缓存、Pillar A-D 复核分级），09-09 方向教义（条件式/先A后B无方向、操作动词=方向同义、仓位状态≠方向）落地；v16 之后版本演进不再逐条铺陈于本表，以 git commit（9142ba6 基线起）+ `archive/20260909-legacy/README.md`（C1 死代码移除清单）为准 |
