# -*- coding: utf-8 -*-
"""05 回测 —— 把**指定博主池**在**回看窗口**内的观点，按**推送那张时刻表**逐档判成
一个仓位，算出一条净值。

    python -m backtest

**代码包与 `blogger/` 同级** —— 回测是独立的一条线，不塞进 `blogger/` 里。
产物落 `backtest/runs/<日期>_<时刻>/`（`回测.md` · `净值.png` · `参数.json`）。

**管什么**：调 04 的全库更新、逐档攒分布、状态机、成交与盯市、指标、报告。
**不管什么**：怎么抓帖（01）／一条帖怎么读出观点信号（02）／单博主报告（03）／
全博主对比（04，本文只调它那一步「全库更新」）／推送（06）。

**只考察上证指数，只取波段**（`idx` 是 `上证指数`、交易日跨度 ≥ 2），超短期不进回测。
**回测不看得分** —— 从 03§3 只借一样：**验证终点**，用来判这条观点过期没有。
"""

from backtest import flow

__all__ = ["flow"]
