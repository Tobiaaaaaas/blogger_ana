# 复制为 .env 并填写（.env 已被 gitignore，绝不提交）
# DeepSeek key：可用父仓库 .deepseek_keys.env，二选一即可（本文件优先）
DEEPSEEK_API_KEY=sk-xxx
# 飞书群自定义机器人 webhook（建群 → 添加自定义机器人 → 复制地址）
# 超短卡 → 旧群
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxx
# 波段卡 → 新群（v13；缺失则该板块记失败，绝不回落 FEISHU_WEBHOOK_URL）
FEISHU_WEBHOOK_URL_SWING=https://open.feishu.cn/open-apis/bot/v2/hook/xxx
# 父仓库路径（默认 briefing/ 的上一级）
# REPO_ROOT=/srv/blogger_ana
